# Flapjack - Roadmap

**Last updated:** 2026-08-06 evening, applying the `aug06_10am` batch's receipt-borne ledger deltas. **`v1.0.11` published**, closing `REL-11`; the release path is exercised end to end for the first time since 2026-06-09. `MIG-18`, `SEC-G5`, `REL-12`, `PUB-GUARD-1`, and now `PL-15` closed. `JOIN-1`'s vendor-credential diagnosis is **falsified** — `P29` is green and the joinable denominator moved from 59 to 61. `MIG-21` is **half-landed**: the console reaches all three providers, but no browser has yet driven a Meilisearch or Typesense migration end to end, because release builds compile the loopback admission seams out — filed as `MIG-22`. `NIGHT-1`'s dashboard half now carries a landed repair. `TEST-FLAKE-1` narrowed and surfaced a second, uncensused red filed as `TEST-FLAKE-2`.

This file owns the open-work ledger. Every row names the command, test, or
receipt that produced its claim. `Active` and `Planned` own scope and evidence;
`Up Next` owns ordering only, so the two cannot desync.

**This file supersedes in place; it does not stack revisions.** Through
2026-08-03 it revised by prepending the new state and retaining "prior revision
follows verbatim", five deep: **286 of its 548 lines were retained history**, so
its tail was stale by default, could contradict its head, and grew every time
anyone corrected it. `git log -p` is the correct owner for that history and
already has all of it. When a claim changes, rewrite the row and name the commit
or receipt that changed it — provenance is a citation, not a retained copy. The
stack was removed on 2026-08-03 with every `Active`/`Up Next`/`Planned` row ID
verified present before and after. See row `DOC-LAYOUT-1`.

**Last shipped release:** [v1.0.11](https://github.com/flapjackhq/flapjack/releases/tag/v1.0.11), published `2026-08-06T11:52:14Z` by prod `release.yml` run `31096713795` at prod-mirror SHA `35da0206f8d5cf567750da8d3c6fcb34859c5c69` (dev source `1b32cf727`), with 15 release assets and GHCR `1.0.11` / `latest` at `sha256:9e704ab0f064c1cc312a6f83bfb38e7c07820162c9e5e69fe7a43b050e752acd`. The unpinned `install.flapjack.foo` installer now resolves `v1.0.11`. This supersedes the 2026-08-05 stopped cut, whose two `unknown-linux-musl` packaging defects are repaired under `REL-11`. Detailed release history lives in [`CHANGELOG.md`](CHANGELOG.md).

**ID prefixes:** `RF-*` = foundational refinement track; `PL-*` =
launch-hardening / operational-polish track; `MIG-*` = source-migration
capability track; `DUR-*` = durability and persistence-correctness track. IDs are
stable identifiers, not priority rank.

## Active

| ID | Work Item | Current State | Evidence / Owner |
|----|-----------|---------------|------------------|
| REL-11 | v1.0.11 release packaging repair and publish — the release path had been structurally broken since 2026-07-15 and 2026-08-05 was its first exercise | **CLOSED 2026-08-06, on every clause of the exit this row named.** Both packaging defects are repaired. `engine/Cross.toml` now exists and names `FLAPJACK_BUILD_REVISION` in `build.env.passthrough`, so the value reaches `cargo` inside the `cross` container, `engine/build.rs` reads it, and a cross-target packaged `build-info --json` reports `revisionKnown: true`. The `aarch64` package path derives its manifest without executing the foreign-architecture binary on the host. `engine/tests/test_release_workflow_structure.sh` asserts delivery rather than YAML text presence and is re-run against an identity-rewritten copy so it cannot be green locally and red on the mirror. A fresh exact-SHA staging matrix run succeeded on all five build jobs, and prod `release.yml` run `31096713795` concluded `success`, publishing tag and GitHub Release `v1.0.11` (15 assets) plus GHCR `1.0.11` and `latest`. Customer-channel proof was taken through public consumer paths only: the published `x86_64-unknown-linux-musl` archive verifies against its `.sha256`, its `build-info --json` reports `version 1.0.11` / `revisionKnown: true` executed from a Linux amd64 locality, and the unpinned installer prints `Latest version: v1.0.11`. **Consequence, stated once: this is the first time since 2026-06-09 that any engine change can reach a user by either channel** — the OSS installer and the Flapjack Cloud engine AMI, which bakes from this same published archive and manifest. **Three defects the cut surfaced are deliberately NOT folded into this closure and carry their own rows — `REL-12` (dispatch ordering), `CI-E2E-1` and `TEST-FLAKE-1` (the two red jobs in prod push CI at the released SHA).** Neither red is a defect in the published binaries, and the green `release.yml` run is not evidence that prod CI is clean; both runs must be read. | Repair receipt: `2026_08_05_aug05_11am_1_release_packaging_repair_receipt.md`; customer-channel publish receipt: `2026_08_05_aug05_11am_2_v1011_publish_receipt.md`, whose Stage 3 re-verification at `2026-08-06T12:57:02Z` re-ran every customer-visible check (unauthenticated release-asset download, `sha256sum -c`, containerised `build-info --json`, GHCR read, unpinned installer resolution) rather than citing Stage 2. Closed owners: `engine/package/release_artifact_manifest`, `engine/Cross.toml`, `.github/workflows/release.yml`, `engine/tests/test_release_workflow_structure.sh`, `engine/build.rs`. Re-verify any claim above live rather than from this row: `gh api repos/flapjackhq/flapjack/releases/tags/v1.0.11` and `gh run view 31096713795 --repo flapjackhq/flapjack`. The downstream `FJCLOUD_FLAPJACK_VERSION` pin bump (`fjcloud_dev`, `scripts/lib/flapjack_binary.sh:21`) is owned by that repository, not here. |
| NIGHT-1 | The public mirror's `Nightly Tests` have failed **30 consecutive runs**, 2026-07-08 through 2026-08-06; the last green run was 2026-07-07 | **OPEN — filed 2026-08-05, restated 2026-08-06 (streak), and again 2026-08-06 evening after the `aug06_10am_2` dashboard-half repair landed. Three of the four attributed failure buckets now carry a landed repair; none has yet been observed green on a scheduled run.** The streak is 30, not the 20 originally filed: `gh run list --workflow nightly.yml --limit 60` shows `failure` for every run back to 2026-07-08 and `success` at 2026-07-07. Window attribution over 2026-07-17 → 2026-08-05 (20 runs, all attributed) found four buckets: migration import selector 14, dashboard success-card timeout 6, invalid-credential message mismatch 2, Clippy 3. (1) *`Migration import contract` — repaired 2026-08-05, unproven.* The receipt selector matched by leading-key anchor while the emitter's key order changed at `9c01b1c75`, so a passing migration exited `1`; it now parses each line as JSON and validates the `migration_import` check, with legacy-anchor, shape-drift, permuted-key, missing-check, and wrong-count controls. The repair reached the prod mirror at `35da0206f`, so **2026-08-07 is the first scheduled run that can exercise it**. (2) *`Dashboard all tests` — repair LANDED 2026-08-06 at `a38465156`, unproven.* Run `31072375322` failed in `cluster_peers.spec.ts` at `tests/fixtures/cluster-peers.ts:117` — "require a replication-enabled HA backend, but the target reports standalone mode". Of the two sanctioned repairs the lane took the first: **the backend is provisioned, the assertion is untouched, and the fixture still throws on a standalone target — deliberately.** `.github/workflows/nightly.yml:143-145` and `.github/workflows/ci.yml:391-393` now export `FLAPJACK_NODE_ID`, `FLAPJACK_ADVERTISE_ADDR`, and `FLAPJACK_REPLICATION_API_KEY` at the backend-start step, `engine/dashboard/tests/e2e_backend_contract.json` is the single declared owner of which capabilities the specs require, and `engine/tests/test_dashboard_e2e_backend_contract.py` (registered in `ci.yml`'s `release-contracts` job) holds every backend-starting job to that contract. (3) *Algolia invalid-credential mismatch — repair landed 2026-08-06 at `8e6ccbe00`*, which makes the Algolia fixture assert its own credential readiness instead of failing downstream on an ambient credential. (4) *Rust Clippy — no repair commit identified and no owner named, but the gate is green at `main` HEAD.* Measured 2026-08-06 evening on darwin-arm64: the nightly job's exact invocation `cargo clippy --workspace --all-targets -- -D warnings -A clippy::too_many_arguments -A clippy::type_complexity -A clippy::only_used_in_recursion -A deprecated` exits `0` with zero warnings, and `cargo fmt --all --check` exits `0`. **That is evidence, not proof — the nightly runs on `ubuntu-latest`, and a lint set can differ by platform and toolchain release.** Do not read a green night as closing this bucket without checking the Clippy job specifically. | Owners: `.github/workflows/nightly.yml`, `engine/tests/migration_import_contract.sh`, `engine/dashboard/tests/e2e-ui/full/cluster_peers.spec.ts`, `engine/dashboard/tests/fixtures/cluster-peers.ts`, `engine/dashboard/tests/e2e_backend_contract.json`. Falsifiable exit, unchanged and covering every bucket: **two consecutive scheduled `nightly.yml` runs conclude `success`.** One green run is not the exit — the workflow is scheduled, so a single pass cannot distinguish a repair from a lucky night. Repair receipts: `engine/docs2/4_EVIDENCE/2026_08_05_aug05_am_2_nightly_migration_oracle_repair_receipt.md` (bucket 1) and the reviewed private nightly skip-denominator receipt (bucket 2 attribution and denominators). Re-measure, never recall: `gh run list --repo flapjackhq/flapjack --workflow nightly.yml --limit 25 --json conclusion,createdAt,databaseId`, then `gh run view <id> --json jobs`. |
| REL-12 | An irreversible release dispatch can begin before prod CI is terminal at the same SHA | **CLOSED 2026-08-06, on the exit this row named.** `.github/workflows/release.yml` now carries a `release_ci_status_preflight` job that resolves the target SHA's ordinary push-CI run and refuses unless it is terminal `success`; the `release` job lists it in `needs:`, so the refusal happens before any tag is cut. It sits beside `ghcr_publish_preflight`, which is the seam that already fails a run before publication. The behavioural contract `engine/tests/test_release_ci_status_preflight.sh` drives a fake `gh` on `PATH` — no network — and proves the refusal fires across terminal-`success`, `in_progress`, `failure`, no-run, and API-indeterminate, keeping each verdict distinct so an indeterminate probe is never reported as a terminal CI verdict. | Owners: `.github/workflows/release.yml` (`release_ci_status_preflight`), `engine/package/release_ci_status_preflight`. Shape contract: `engine/tests/test_release_workflow_structure.sh`. Behavioural contract: `engine/tests/test_release_ci_status_preflight.sh` — **it landed invoked by no workflow and was wired into `ci.yml`'s `release-contracts` job afterwards**, with a self-detecting assertion in the structure test so unwiring it reds the gate. That inert-contract failure is the one the structure test's own header records; it recurred here. |
| CI-E2E-1 | Prod CI's `Dashboard full e2e tests` job is red on a CI-harness env gap, not a product defect | **OPEN — the repair has LANDED and was extended 2026-08-06; only its mirror observation is outstanding. Do not re-implement it.** In prod push CI run `31096601354` at the released SHA, `engine/dashboard/tests/e2e-ui/full/vector-settings.spec.ts` failed three times with `configureEmbedder failed (400): rest embedder URL private or local destination `127.0.0.1` is not allowed`. That refusal is a correct fail-closed SSRF control with a documented operator opt-in (`engine/src/security.rs::allow_local_outbound_urls` reads `FLAPJACK_AI_ALLOW_LOCAL_URLS`); the harness simply did not set it. **Root cause, shared with `NIGHT-1`'s dashboard half:** `startPlaywrightServers` passes `allowReuse: true` for the backend, so a workflow that starts one first has it reused and `spawnBackendServer` — the only place that set the environment — never runs, which is why env on the Playwright step cannot reach the process under test. Landed at `c3b81af18` and hardened at `a38465156`: both `ci.yml` and `nightly.yml` carry the opt-in at the backend-start step, a contract with an absent variable now forces a fresh backend rather than silently reusing an incapable one (`requireFreshProcess`), and a non-loopback bind under the public dev admin key is refused outright (`5195cc96a`). **What remains is exactly one thing: observing the job green on the prod mirror at a SHA where it previously failed.** | Owner: `.github/workflows/ci.yml`, the `Run full e2e tests` job's backend startup step. Contract test: `engine/tests/test_dashboard_e2e_backend_contract.py`, run in `ci.yml`'s `release-contracts` job; it holds every job that starts a backend and then runs dashboard specs to the declared contract, asserts its own invocation so unwiring it reds, and resolves each npm script's actual spec selection per Playwright invocation rather than per script. **Do not close this by weakening `security.rs`** — that deletes a real control to make a harness pass. Mirror-identity trap from `DOC-PUBLIC-1` still applies: verify against the mirror's copy of `ci.yml`. |
| TEST-FLAKE-1 | `merge_owner_survives_consecutive_commits` fails nondeterministically in union runs | **OPEN and NARROWED 2026-08-06 by `aug06_10am_4`. The signal-based wait landed (ARM B) and the target passed the detached full-lib specimen, but union absence is unproven and the broad gate is not green.** `index::write_queue::tests::merge_owner_survives_consecutive_commits` panicked at `engine/src/index/write_queue_tests.rs:3245` and took down `Rust tests (all)` in prod push CI run `31096601354` at the released SHA. The lane replaced the timing-dependent wait with a worker merge signal (`ba0101652`) and the target then passed in the detached full-lib run. **Two things block closure and neither is the target test.** First, the allowed Stage 4 detached full-lib gate exited `124` (timeout) rather than `0`, so no clean broad run exists to prove workspace-union absence. Second, that run exposed an **uncensused** red now carried separately as `TEST-FLAKE-2`. Standing discriminator, unchanged: **before attributing a union-only red to a product defect, rule out the stale incremental cache** with one run from a quarantined `engine/target` at `CARGO_INCREMENTAL=0` — not another union on the same tree. | Owners: `engine/src/index/write_queue.rs`, `engine/src/index/write_queue_tests.rs`. Census: `engine/docs2/4_EVIDENCE/2026_08_05_aug05_12pm_6_union_only_nondeterminism_census.json`; narrowing receipt: the reviewed private write-queue merge-convergence determinism receipt. Falsifiable exit, unchanged: a deterministic reproducer that is red before the fix and green after, plus **one broad gate that exits `0` rather than `124`**, with no `#[serial]`, no new mutex, and no weakened assertion. |
| PUB-GUARD-1 | Nothing in this repository refuses a mirror publish sourced from an unmerged worktree | **CLOSED 2026-08-06 at `b9b592da2`, on the exit this row named, and extended the same day at `4b64a7689`.** `scripts/lib/publish_guard.sh` is sourced unconditionally by `.debbie/post-sync.sh` — first, before the hook mutates the mirror worktree, so a refusal cannot fire after the tree has already been stripped and rewritten — and refuses when the invoking `HEAD` is not an ancestor of `origin/main`. Ancestry is the whole predicate on purpose: a branch-name check would reject `detached at a pushed origin/main SHA`, which is the *only* correct way a lane can publish. 23 behavioural cases run against real throwaway git repositories rather than grepping the guard's source; three mutations were each confirmed red (inverting the ancestry predicate reds 6 cases, returning allow on an unresolvable `origin/main` reds 1, moving the guard after the mirror mutation reds the ordering case). `4b64a7689` then closed the hole this row's guard header had explicitly named as out of scope: debbie copies from the working tree, so a dirty synced path is now refused too. | Owner: `scripts/lib/publish_guard.sh`, wired at `.debbie/post-sync.sh`. Behavioural contract: `scripts/tests/publish_guard_test.sh`, registered as `./s/test --publish-guard` and included in `--all` / `--ci`. **That runner is its only automatic runner** — `scripts/` and `.debbie/` are outside debbie's sync scope so mirror CI cannot run it, and dev repos keep Actions disabled. `engine/_dev/s/test` IS remapped to `engine/s/test` on both mirrors, so the suite branches on `.debbie.toml`, verified absent on `flapjackhq/flapjack` by reading the mirror rather than by reading `.debbie.toml`; the dev arm is fail-closed because in a clone that can publish, a missing guard is the defect. |
| ING-1 | OSS ingestion CLI and control-plane progression | In progress. The shipped beta is a client-side `flapjack ingest` slice for JSON arrays, NDJSON files, and stdin, with upsert plus explicit object-ID deletes; it is not the parked hosted scheduler/worker control plane. | Runtime owner: `engine/flapjack-server/src/ingest.rs`; executable contract: `engine/flapjack-server/tests/ingest_cli_test.rs`; public bounds: `engine/docs2/FEATURES.md#flapjack-ingest-beta-bounds`. |
| MIG-5 | Migration `overwrite=true` into an existing target | Shipped for node-local synchronous and authenticated async migration, including streamed NDJSON admission at `POST /1/migrations/bulk-replace`. All paths route through the canonical fenced replacement owner after validating the committed journal generation, durable target epoch `E_new`, and promoted target `committed_seq = W`; the bulk endpoint's receipt and status state `single_node_only`, and HA admission returns `503 migration_ha_unsupported`. Real-server synchronous evidence: [node-local overwrite summary](engine/docs2/evidence/2026_07_24_mig5b_node_local_overwrite/summary.md). Async known-answer evidence: [replacement/publication repair receipt](engine/docs2/4_EVIDENCE/2026_07_25_jul25_am_4_async_migration_replace_publication_repair_receipt.md). | Design reference: [ADR 0008](engine/docs2/3_IMPLEMENTATION/decisions/active/0008_mig5_overwrite_mutation_fence_design.md). Replacement owner: `engine/src/index/manager/lifecycle.rs`; publication owners: `engine/src/index/manager/publication.rs` and `engine/src/index/manager/publication/**`; mutation fence owners: `engine/src/index/manager/write.rs`, `engine/src/index/write_queue/`; migration routing owners: `engine/flapjack-http/src/handlers/migration/{bulk_replace,job_runner,spool}.rs`. |
| MIG-6 | Async migration job contract (status / cancel / resume) | **Shipped for Algolia (2026-07-31).** An interrupted pre-publication export can be claimed through the explicit admin-authenticated `POST /1/migrations/{provider}/{job_id}/resume` route using fresh request-only credentials; positive status exposes `resumable`, `operation`, and `resumeHandle`; interruption preserves the original absolute `expires_at` with no second GC owner; exact-ID and full-process-restart contracts prove exactly-once continuation. Meilisearch and Typesense resume remain unsupported. Row retained in Active only until a release carries it. | Owners: `engine/flapjack-http/src/handlers/migration/{spool.rs,spool_lifecycle.rs,spool_support.rs,export.rs,job_runner.rs,mod.rs,import.rs,algolia_client.rs}`, `engine/flapjack-http/src/{router.rs,openapi.rs}`; restart proof: `engine/flapjack-server/tests/crash_durability_test.rs::interrupted_async_migration_resumes_exactly_once_after_process_restart`; design: [ADR 0012](engine/docs2/3_IMPLEMENTATION/decisions/active/0012_mig6_resume_interrupt_state_design.md); evidence: the reviewed private mig6 resume receipt. |
| DUR-1 | Durable admission under disk exhaustion | **CLOSED 2026-08-04.** The class-shaped append-I/O failure proof remains green, and the remaining current-HEAD fill-disk instance passed three sequential real-filesystem trials at exact fetched `origin/main` SHA `3b11f8216f1d7dccc74262e1b63b3e1603152202`. All three specimens were accepted both at the detached measurement tree and from the merge-current lane tree; each records `outcome=PASS`, `acknowledged_count=28`, `recovered_count=28`, `rejection_status=500`, `script_exit_code=0`, `interrupted_exit_code=0`, and `source_sha` equal to the measurement SHA. Post-measurement fetch found `origin/main` unchanged and the diagnostic diff empty. Receipt: the reviewed private dur1 respecimen and close receipt. | Class owner: `engine/src/index/write_queue_tests.rs::oplog_append_io_failure_before_acknowledgement_is_fail_closed`; producer: `engine/_dev/s/manual-tests/20260730_disk_exhaustion_durability.sh`; acceptance owner: `engine/loadtest/tests/disk_exhaustion_acceptance.sh`. Regression exit: build the release server, run three sequential bounded fill-disk trials at one current SHA, and require explicit acceptance plus exact `summary.json.source_sha` equality for every trial. |
| DUR-2 | S3 snapshot non-success response propagation | **CLOSED 2026-08-02 by the `aug02_11am_3` lane.** `engine/src/index/s3.rs::list_snapshots` now checks every ListObjectsV2 HTTP response status before parsing the body and returns `FlapjackError::S3("S3 list: HTTP <status>")` instead of a downstream `ListBucketResult` parse error. The row's own closure contract was run verbatim: `cargo test -p flapjack --lib -- --list` contains all three of `upload_snapshot_fails_loudly_when_bucket_rejects_the_put`, `delete_snapshot_fails_loudly_when_bucket_rejects_delete`, and `list_snapshots_fails_loudly_when_bucket_rejects_list` (each `grep -c` returned `1`), and `timeout 600 cargo test -p flapjack --lib -- index::s3::tests` reported `ok. 14 passed; 0 failed`. Full library `--no-fail-fast` was `2200 passed; 0 failed`, with clippy, `fmt --check`, and the MinIO snapshot harness green. Implementation note recorded so it is not re-litigated: `rust-s3` keeps XML deserialization internal, so the lane made already-transitive `quick-xml 0.38` a direct optional dependency wired only to `s3-snapshots`, rather than issuing a second request after the raw status probe. Receipt: `engine/docs2/4_EVIDENCE/2026_08_02_aug02_11am_3_s3_list_failure_propagation_receipt.md`. Route this row to the private implementation history at the next history pass. **Original filing follows.** OPEN — upload and delete are fixed; list remains unproved. The `jul31_2pm_3` lane shipped the upload status check and `upload_snapshot_fails_loudly_when_bucket_rejects_the_put`; later tree work shipped the delete status check and `delete_snapshot_fails_loudly_when_bucket_rejects_delete`. `list_snapshots` still has no focused non-success regression and can surface a misleading list/parse error instead of the rejected HTTP status. | Owner: `engine/src/index/s3.rs::{upload_snapshot,delete_snapshot,list_snapshots}`. Closure contract: first require `cargo test -p flapjack --lib -- --list` to contain `upload_snapshot_fails_loudly_when_bucket_rejects_the_put`, `delete_snapshot_fails_loudly_when_bucket_rejects_delete`, and `list_snapshots_fails_loudly_when_bucket_rejects_list`; then run `timeout 600 cargo test -p flapjack --lib -- index::s3::tests`. An absent per-function test or zero-test filter is red. |
| JOIN-1 | Backend↔frontend joined proof for the dashboard | **Last clean measurement 2026-08-06 at `05c546ca5ba3b8dc92b0cb83e6604f09a7c6c433`: `59 / 59` joinable dashboard rows passing — 0 failed, 0 skipped, 0 not-run, 0 unresolved, all 28 proof keys green. That measurement satisfied the exit predicate at that SHA. It does not close the row, because the denominator moved the same afternoon and the two rows it gained are red.** **The vendor-credential diagnosis is FALSIFIED and must not be re-inherited.** The 2026-08-05 sweep receipt attributed `P29` (`full/migrate-algolia.spec.ts`) to an Algolia-side refusal that "no repository SHA can fix" and that "requires an external action". `aug06_10am_5` walked all four hops with the canonical pair from `engine/.secret/.env.secret` and every one passed: shell→vendor HTTP `200` with `2` indexes on both the DSN and control hosts; `AlgoliaClient::new`/`list_indexes` `1 passed`; the server route `POST /1/algolia-list-indexes` HTTP `200`; and the Playwright fixture `4 passed`, including the success path and the invalid-credential UI path. **`P29` is green.** The failure was never revision-independent, and a `403` `Invalid Application-ID or API key` body is byte-identical for an empty key, a bogus key, and a genuine vendor refusal — so it can never by itself attribute a failure to the vendor. **Current state: the denominator is `61`, not `59`, and 2 of the new rows are red.** `MIG-21`'s console half merged at `63cd2c54d` (2026-08-06 16:11) and registered proof keys `P30` (`full/migrate-meilisearch.spec.ts`) and `P31` (`full/migrate-typesense.spec.ts`) in the manifest, taking `rows` from 90 to 92 and dashboard-route rows with a proof key from 59 to 61. Both new keys fail today, on the backend prerequisite carried as `MIG-22` — a console defect is **not** the cause. This increase is the predicate working as designed, not a regression. The composition at the current manifest: 92 rows = 65 dashboard-route (61 with a proof key, 4 without a candidate spec) + 19 API-only + 7 config-only + 1 CLI-only. | Matrix owner: `engine/docs2/4_EVIDENCE/2026_07_30_jul30_12am_3_dashboard_join_audit_receipt.md`; machine join owner: `engine/dashboard/scripts/join_proof_report.mjs`; manifest: `engine/dashboard/tests/e2e-ui/join_proof_manifest.json`; current receipt: the reviewed private JOIN-1 predicate receipt; four-hop falsification of the credential diagnosis: the reviewed private Algolia credential hop findings. **Two prior corrections, both explicitly dispositioned here so neither is re-applied.** The denominator method — joinable rows, not backend rows — originated in [`engine/docs2/4_EVIDENCE/2026_08_02_aug02_11am_6_dashboard_joined_proof_receipt.md`](engine/docs2/4_EVIDENCE/2026_08_02_aug02_11am_6_dashboard_joined_proof_receipt.md) and **still stands**; only that receipt's zero numerator is superseded. The 2026-08-05 vector-enabled sweep receipt `2026_08_05_aug05_12pm_3_join_proof_vector_enabled_sweep_receipt.md` asked for a `56 / 59` value and a vendor-credential root cause: the value is superseded by the `59 / 59` re-measurement and **the root cause is refused as falsified** — a deliberate no-change recorded here rather than applied; public capability owner: [`engine/docs2/FEATURES.md`](engine/docs2/FEATURES.md). Falsifiable exit, stated as a predicate rather than a frozen integer because the joinable denominator moves whenever the console gains a capability: run the full browser suite at one clean audited SHA, then `cd engine/dashboard && node scripts/join_proof_report.mjs`; closure requires **0 failed, 0 skipped, 0 not-run, and 0 unresolved proof keys against the manifest's own denominator at that SHA**, with the denominator and SHA both recorded. **Do not copy `59` or `61` forward — re-run the report.** A capability-gated skip is never counted toward the numerator. |
| MIG-21 | The console migrates from one of the three source providers the backend supports, and offers no dry-run | **OPEN and HALF-LANDED. The provider half merged 2026-08-06 at `63cd2c54d`; the dry-run half is unstarted; and the provider half's browser proof is blocked on `MIG-22`, not on any console defect.** What shipped: `engine/dashboard/src/pages/migrateHelpers.ts:28` is now a three-entry `MIGRATION_PROVIDER_DESCRIPTORS` array (`algolia`, `meilisearch`, `typesense`), `Migrate.tsx` drives discovery, submit, and polling off the descriptor's `routeSegment` against the real `/1/migrations/{provider}` routes instead of the legacy Algolia compat aliases, unknown Algolia migration fields are now rejected (`a2f0e671a`), jobs poll to a terminal state (`706420419`), and two new browser specs exist — `engine/dashboard/tests/e2e-ui/full/migrate-{meilisearch,typesense}.spec.ts`, registered as `P30`/`P31` in the join-proof manifest. Non-browser gates are green at HEAD: dashboard unit 101 files / 915 tests, `tsc --noEmit` `0`, `eslint` `0`. **The honesty requirement is met and independently proven:** `npm run test:e2e-ui:migrate-meilisearch-refusal` passes 3, verifying the console renders the SSRF refusal and names `FJ_ENABLE_MEILISEARCH_PREVIEW_LOOPBACK` rather than a generic error. **What is NOT proven, stated plainly: no browser has yet driven a Meilisearch or Typesense migration end to end.** Both provider specs run and fail — they do not skip, so no skip hides the deliverable — because the release backend the e2e harness builds compiles the loopback source-reader admission out. That is `MIG-22`, owned by lane `aug06_10am_1a`. **Do not read the descriptor array landing as the capability being proven.** The **dry-run half is untouched**: no console path reaches `preview` for any provider, so the one migration capability measurably ahead of both Meilisearch's and Typesense's own migration guides still has no console surface. Externally verified 2026-08-06 and unchanged: Typesense Cloud's dashboard has no collection-import feature at all, and Meilisearch's Algolia-migration guide hands you a record loop to write yourself. | Owners: `engine/dashboard/src/pages/{Migrate.tsx,MigrateSections.tsx,migrateHelpers.ts}`, the private migrate screen contract (created; its "Current implementation gaps" section is the live gap record), `docs/screen_specs/_audit.md` row `AUD-013`, `engine/dashboard/tests/e2e-ui/join_proof_manifest.json`. Dry-run half: the private `aug06_10am_7` console-migration dry-run checklist, authored and **not dispatched** — its `Dispatch-after:` gate requires the `aug06_10am_1a` lane merged. **Reuse, do not reinvent:** `engine/tests/source_migration_provider_parity_http_probe.sh` already boots digest-pinned loopback-only Meilisearch and Typesense containers, seeds them, and proves migrated documents through the served search API. Falsifiable exit, unchanged: for **each** of the three providers, a real browser drives the console through source discovery, a translation-report dry-run, and a submit against a real source, and the migrated documents are then proven through the served search API; the 390px audit (`engine/dashboard/tests/e2e-ui/stage3_route_audit_390.spec.ts`) and the full `e2e-ui` suite both stay green; and a source on a private address renders the named opt-in rather than a generic error. Receipts: the reviewed private console-provider red receipt and its companion red-evidence record. |
| MIG-22 | Release binaries compile out the Meilisearch and Typesense loopback admission seams, so no console migration from a self-hosted source can be proven | **OPEN — filed 2026-08-06 from `aug06_10am_1`'s blocked Stage 2, with falsifiable snapshot evidence. Repair in flight in lane `aug06_10am_1a`.** The Meilisearch submit and Typesense discovery/submit loopback fallbacks are `#[cfg(debug_assertions)]`-gated in `engine/flapjack-http/src/handlers/migration/mod.rs` (`:2223,2331,2342,2361,2388,2414`) and `typesense_client.rs` (`:138,146,155,367,375`). `npm run update-server` builds `--release`, so the documented process opt-ins `FJ_ENABLE_MEILISEARCH_PREVIEW_LOOPBACK=1` / `FJ_ENABLE_TYPESENSE_PREVIEW_LOOPBACK=1` **cannot take effect in the binary the dashboard e2e harness actually runs**. **This is a documentation/behaviour contradiction, not only a test-harness inconvenience:** the console correctly tells a self-hosting user to set an environment variable that the shipped build ignores. Literal red evidence: Meilisearch discovery reached the seeded source (`2 records`) and submit returned `Meilisearch Cloud endpoint is not allowed`; Typesense discovery returned `Typesense Cloud endpoint is not allowed` before a collection could be selected. Falsifiable snapshot proof that the gating is the cause: the release-loopback gate archived from `df12843f4` produced 4 PASS / `Total failures: 0`, and the same gate against `df12843f4^` produced 5 targeted failures / `Total failures: 5` — both via `git archive`, with no working-tree modification. | Owners: `engine/flapjack-http/src/handlers/migration/mod.rs`, `engine/flapjack-http/src/handlers/migration/typesense_client.rs`, and their tests; contract owner `engine/tests/migration_release_loopback_contract.sh`. In-flight lane: the private `aug06_10am_1a` release-loopback source-contract checklist. Live gap record: the private migrate screen contract, "Current implementation gaps". Falsifiable exit: a `--release` backend started with both opt-ins set admits a loopback Meilisearch and Typesense source, `engine/tests/migration_release_loopback_contract.sh` exits `0` and is registered in a recurring gate, and **the opt-ins absent still refuse** — the release build must gain the opt-in path without losing the fail-closed default. Closure additionally requires `P30` and `P31` green in `node scripts/join_proof_report.mjs`. |
| TEST-FLAKE-2 | `bounded_aggregate_concurrency_across_simultaneous_requests` is red in a detached full-lib run and is not in the union census | **OPEN — filed 2026-08-06 by `aug06_10am_4`, which surfaced it and correctly declined to fix it out of scope.** `query::executor::bounded_tests::bounded_aggregate_concurrency_across_simultaneous_requests` (`engine/src/query/executor/bounded_tests.rs:297`) went red in the Stage 4 detached full-lib run, and **it is not a member of the `aug05_12pm_6` union-nondeterminism census** — so the standing "it is a known union flake" disposition does not cover it and must not be extended to it by analogy. The same run exited `124`, so it is not yet known whether this is deterministic or timing-dependent. It matters beyond itself: it is the reason `TEST-FLAKE-1` cannot claim a green broad gate, and the repo's wave-transition gate is `cargo test --workspace`. | Owners: `engine/src/query/executor/bounded_tests.rs` and the bounded-aggregate executor path it drives. Surfacing receipt: the reviewed private write-queue merge-convergence determinism receipt. Falsifiable exit: the failure is classified deterministic or nondeterministic **with a `%cpu` reading and a `sample` stack for any 0%-CPU hang**, either repaired with a reverted-red proof or promoted into the census with its interaction seam named, and one broad gate then exits `0`. Do not raise a timeout in place of the diagnosis. |
| MIG-7 | HA-converging import | Refused by design, not planned for v1. [ADR 0009](engine/docs2/3_IMPLEMENTATION/decisions/active/0009_mig7_ha_converging_import_design.md) records the corrected baseline: `move_index` / `move_index_with_publication` preserves the source oplog and `committed_seq`; the HA gap is node-local publication plus no durable cross-node convergence epoch, exclusive promotion rule, or peer adoption receipt. Reopen only with the costed pull-snapshot adoption design in ADR 0009. | `engine/src/index/manager/publication.rs`; refusal owner: `engine/flapjack-http/src/handlers/migration/mod.rs::admit_migration_request` |
| RF-4 | Runbooks iteration | Open-ended operational follow-through. Continue refining runbooks from incident learnings. On 2026-07-21, the [Migration jobs](engine/docs2/3_IMPLEMENTATION/OPERATIONS.md#migration-jobs) operations runbook landed; no next RF-4 runbook candidate surfaced during the migration probe. | [`engine/docs2/3_IMPLEMENTATION/OPERATIONS.md`](engine/docs2/3_IMPLEMENTATION/OPERATIONS.md) |
| PL-10 | Write-path saturation under sustained load | **OPEN for v1.1 architecture work; the single-machine scale record remains uncorrected, but the 2026-08-03 reference-scale reason changed.** The remote reference harness survived archive dispatch, instance-store locality verification, dataset generation, import, liveness, evidence capture, and teardown. On an `i4i.4xlarge` at 1,000,000 documents, the frozen contract then reached a genuine measured ceiling: facet-query p95 was `100.18` ms against the `100` ms per-query-type limit, while every other capacity and latency gate passed. Because that rung was not green, no scale number was published; the next measurement lane should begin from the facet-latency diagnosis. The existing 1-million-record proxy remains load-contaminated (`804.49` docs/s), and a larger single-machine rung would not resolve the single-writer Tantivy mutex or cross-node fanout. Receipt: the reviewed private scale ladder survivability receipt. | Prior measured floor: `engine/docs2/4_EVIDENCE/2026_08_02_aug02_5am_1_ladder_salvage_and_gate_disposition.md`; v1.1 design: [ADR 0007](engine/docs2/3_IMPLEMENTATION/decisions/active/0007_pl10_v11_write_path_design.md). Falsifiable exit: before another bulk run, a timed unit-throughput probe in the same production locality must project the total within the stage budget; a full run must publish its measured rung, while any failed rung changes no published floor. |
| HA-FLAKE | HA snapshot test flake remediation | Fix verified and leaky-pass sites closed; keep future HA regression signal protected. Not v1.0.x blocking. | Fix owner paths: `engine/flapjack-http/src/startup_catchup.rs`, `engine/flapjack-replication/src/manager.rs`, `engine/src/analytics/writer.rs`; regression contract: `engine/tests/test_snapshot_import_failure_contract.rs`; proof: the private may31 eve ha snapshot flake verify proof research note |
| HA-AUTOHEAL | Dead-node auto-heal engine capability | Shipped as a bounded engine capability and disabled by default. **Local Docker decision contract proven (2026-07-24):** `engine/examples/ha-cluster/test_ha_autoheal.sh` proves enabled majority-loss refusal, legal single-node eviction, readmission with startup catch-up, and disabled refusal; the passing transcript is retained under `engine/examples/ha-cluster/.evidence/20260724T215953Z/`. With `FLAPJACK_AUTOHEAL_ENABLED=true`, sustained-unreachable single-node eviction is guarded by the three-observation threshold and the local quorum/partition refusal rule; returning healthy candidates are readmitted through the membership owner with startup catch-up before authoritative reads. It is not consensus, majority writes, arbitrary partition healing, or simultaneous majority-loss recovery; behavior details route to [Dead-node auto-heal](engine/docs2/3_IMPLEMENTATION/OPERATIONS.md#scenario-dead-node-auto-heal) and [High Availability](engine/docs2/FEATURES.md#high-availability). | Policy owner: `engine/flapjack-replication/src/autoheal.rs`; observe/act/readmission owners: `engine/flapjack-replication/src/manager.rs::{apply_autoheal_probe_pass,record_autoheal_eviction,readmit_healthy_autoheal_candidates}`; status owner: `engine/flapjack-http/src/handlers/internal.rs::cluster_status`; live proof: `deploy/helm/flapjack/test_live_cluster.sh`. |
| PL-8 | Nginx restart-window write-loss recovery residual routing | Core restart-window write-loss fix is closed; residual tracking remains here so HA docs keep one open-work owner. HA convergence posture is **bounded convergence** after L1 anti-entropy fix `066549d5`; remaining saturation routes to PL-10 and cross-node idempotency routes to ADR-0005 OQ4. | Canonical evidence owner: [`engine/loadtest/BENCHMARKS.md`](engine/loadtest/BENCHMARKS.md); narrative seam: `engine/docs2/3_IMPLEMENTATION/decisions/active/0004_ha_convergence_reversal.md` |
| SEC-W1 | Security wave 1 — request-handling robustness and browser hardening | **CLOSED 2026-08-01.** Credential-parser robustness, admin-query credential refusal, fail-closed route authorization, global security headers including CSP, and request timeout / concurrency / panic containment all shipped. The last item — the dashboard admin key in `localStorage` — was accepted as a documented risk on 2026-08-01. **That acceptance was revoked 2026-08-02 and `SEC-G3` reopened as its own row below; `SEC-G3` then closed 2026-08-03 on its named cookie-session exit.** SD-009's entire load-bearing premise was "the React directory is scheduled for deletion, not repair"; the operator reversed the deletion, so the premise was false. Its technical analysis was reused by the shipped `SEC-G3` implementation. `SEC-W1` stays CLOSED for its other five items. | Public probes: `engine/tests/credential_parser_http_probe.sh`, `engine/tests/authorization_boundary_http_probe.sh`, `engine/tests/security_headers_http_probe.sh`, `engine/tests/csp_dashboard_browser_probe.sh`, `engine/tests/resource_bounds_http_probe.sh`; superseded-risk record: the private DECISIONS security record SD-009; register entry: the private CONTROLS security record under Application security; closure owner and proof: `SEC-G3`. |
| SEC-W2 | Security wave 2 - optional in-binary TLS listener | **Static-file listener shipped 2026-08-01.** Operators can start the binary with both `--ssl-cert-path` / `FLAPJACK_SSL_CERT_PATH` and `--ssl-key-path` / `FLAPJACK_SSL_KEY_PATH` to serve HTTPS from startup-loaded PEM files; with both unset, plaintext HTTP remains the default. Startup fails before binding when TLS material is unreadable, malformed, incomplete, or mismatched, and the startup banner reports `https` when TLS is active. Reverse-proxy TLS remains the preferred production topology. This row is the shipped static listener only; ACME-backed in-binary rotation is split to `SEC-W2A`. Stage 3 served probe: `engine/tests/tls_listener_http_probe.sh`; Stage 4 receipt: the reviewed private in binary tls receipt. | Runtime owners: `engine/flapjack-http/src/server.rs` and `engine/flapjack-http/src/tls_serve.rs`; CLI/env owner: `engine/flapjack-server/src/main.rs`; operator owner: [`engine/docs2/3_IMPLEMENTATION/OPS_CONFIGURATION.md`](engine/docs2/3_IMPLEMENTATION/OPS_CONFIGURATION.md); baseline owner: [`engine/docs2/3_IMPLEMENTATION/SECURITY_BASELINE.md`](engine/docs2/3_IMPLEMENTATION/SECURITY_BASELINE.md). |
| SEC-W2A | ACME-backed in-binary TLS lifecycle | **CLOSED 2026-08-04 at `fd656649e`.** The material half persists issued private keys beside full chains, publishes each pair as one owner-private fsynced generation, removes the nginx reload assumption, and passes Pebble DNS/IP known-answer tests. The serving half consumes that same generation through a reloadable rustls resolver: a running listener served the before, renewed, and restored certificates on real handshakes without PID or process-start-time change; malformed publication kept the last valid certificate live; plaintext HTTP-01 challenges remained reachable while other plaintext API requests stayed rejected. | Material receipt: `engine/docs2/4_EVIDENCE/2026_08_03_aug03_11am_3_acme_material_lifecycle_receipt.md`; served-rotation receipt: `2026_08_03_aug03_11am_7_tls_hot_reload_receipt.md`. Falsifiable exit: `cd engine && timeout 600 cargo test -p flapjack-http --lib --no-fail-fast -- tls_serve`, `timeout 60 bash engine/_dev/s/tests/tls_rotation_probe_contract_test.sh`, and the served probe in the L7 receipt must all remain green; a restart, PID change, malformed-material outage, or lost HTTP-01 reachability is red. |
| SEC-W3 | Security wave 3 — security audit event coverage | **Shipped 2026-08-01, closing gap `SEC-G4`.** The vocabulary went from two event types to eleven actions — `authenticate`, `create_key`, `update_key`, `delete_key`, `restore_key`, `generate_secured_key`, `delete_index`, `set_settings`, `import_snapshot`, `restore_snapshot_from_s3`, `rotate_admin_key` — over two outcomes (`success`, `failure`), with actor/action/target/outcome fields and a bounded route-template mapper so no key, header, or query payload content reaches an event. Emission was consolidated into one owner rather than scattered per handler. Per `SD-006` the engine emits events and does not own durable retention, so this does **not** by itself close the corresponding fjcloud control. Row retained in Active only until a release carries it. | Emitter owner: `engine/flapjack-http/src/security_audit.rs` (`AuditAction`, `AuditOutcome`, `emit_auth_failure` / `emit_auth_success` / `emit_admin_action` / `emit_set_settings_action`); call sites: `engine/flapjack-http/src/auth/middleware.rs`, `engine/flapjack-http/src/handlers/{internal,keys,indices,settings,snapshot}.rs`; served probe: `engine/tests/security_audit_events_http_probe.sh`; contract: `engine/tests/test_security_audit.rs`; receipt: the reviewed private security audit event coverage receipt; decisions: the private DECISIONS security record |
| SEC-W4 | Security wave 4 — hardening and hygiene backlog | **Updated 2026-08-03 — both `SEC-G9` residuals closed, and so did the third defect that receipt named as untracked.** (1) *The peer credential is no longer optional.* `engine/flapjack-http/src/startup.rs::validate_replication_peer_credential` refuses to start a node that configures replication peers without `FLAPJACK_REPLICATION_API_KEY`. (2) *Peer transport is no longer cleartext by default.* `engine/flapjack-replication/src/config.rs::NodeConfig::validate_credentialed_peer_transport` is the single policy owner and refuses an `http://` peer origin that would carry a credential, across static, persisted, bootstrap, and runtime `POST /internal/cluster/peers` paths, unless `FLAPJACK_ALLOW_CLEARTEXT_REPLICATION_PEERS=1` is set explicitly; `analytics_cluster.rs::validate_authenticated_query_peer_transport` wraps the same owner for authenticated query fan-out, and `NodeConfig::retain_transport_safe_bootstrap_peer` keeps bootstrap filtering consistent. (3) *`push_rollup_to_peers` is authenticated.* `AnalyticsClusterClient::with_peer_identity` carries the configured credential on background rollup fan-out, contract-pinned by `push_rollup_to_peers_uses_query_peer_auth_header_contract`; the effective outbound credential is selected once in `server.rs::initialize_server_infrastructure`. A follow-up regression at `aug03_5am_0` proved the runtime endpoint refuses cleartext caller credentials by default with the exact `400` message, serialized under `#[serial_test::serial(runtime_peer_transport_env)]`, and a load-bearing mutation (temporarily setting the escape hatch) turned it red. Receipts: `engine/docs2/4_EVIDENCE/2026_08_02_aug02_11am_4_replication_peer_identity_receipt.md`, `engine/docs2/4_EVIDENCE/2026_08_03_aug03_5am_0_runtime_peer_transport_regression_receipt.md`. Operator upgrade path: the rolling-upgrade runbook in `engine/docs2/3_IMPLEMENTATION/OPERATIONS.md`. **`SEC-W4`'s remaining scope is now exactly two items:** the moderate-advisory supply-chain disposition, and the plaintext `export_to_tarball` / `import_from_tarball` helper residual (`SEC-G5`) tracked in the private CONTROLS security record — which has been carried unscheduled across four consecutive batches and should either get a lane or be explicitly accepted. One deferred test-fixture follow-up is queued in the private follow-ups note under `s69_review_bugs_deferred-cleartext-peer-fixtures`. **Original filing follows.** Partially shipped 2026-07-31. Closed or moved to implemented: analytics client-IP minimization before persistence, fixed non-root container runtime, and request-resource bounds that were pulled forward with the router middleware work. Supply-chain posture narrowed: the bundled dashboard now has a high-and-above production audit gate, while production moderate advisories remain open below that threshold. **Updated 2026-08-01 — both measured halves of `SEC-G5` narrowed materially.** Snapshot uploads now set server-side encryption (`AES256` by default with no env var set, `aws:kms` with optional key id via `FLAPJACK_S3_SSE` / `FLAPJACK_S3_SSE_KMS_KEY_ID`) and verify the returned SSE header rather than trusting the request; the S3 SSE probe was green (`CONTRACT_EXIT=0`, 16/16). Portable snapshot bytes now support optional AES-256-GCM-SIV encryption via `FLAPJACK_SNAPSHOT_KEY_FILE`; the snapshot-byte symmetry probe was green (`CONTRACT_EXIT=0`, 4/4). The separate plaintext `export_to_tarball` / `import_from_tarball` helper scope remains open in the private CONTROLS security record, but `SEC-G5` is removed from this wave's measured remaining list. **Remaining W4 items:** the `SEC-G9` residuals (make the peer credential mandatory and encrypt peer transport) and the moderate-advisory supply-chain disposition. The configured peer credential's administrative refusal is demonstrated, but does not close either residual. **Updated 2026-08-02 — the `SEC-G9` peer-credential *tier* shipped and merged at `675392367`.** `engine/flapjack-http/src/auth/route_acl.rs::required_acl_for_route` now returns one peer-allowed-or-admin-only decision per routed `/internal/*` method and path; enforcement is header-only with constant-time peer matching in `engine/flapjack-http/src/auth/middleware.rs`; admin-key and no-auth compatibility are preserved for rolling upgrade; and `engine/flapjack-replication/src/peer.rs` carries the peer credential outbound. The served probe proves a configured peer credential replicates while being refused on `add_cluster_peer`, `remove_cluster_peer`, and `rotate_admin_key`. **Both residuals stay open and are now the whole of `SEC-G9`:** `PeerClient::new` takes `peer_credential: Option<String>` (`peer.rs:35`), so an operator who configures replication without a peer credential still gets the pre-change posture, and peer transport is still cleartext `http://`. A third defect is named out of scope by the receipt and is not tracked anywhere else: `analytics_cluster.rs::push_rollup_to_peers` remains an unauthenticated rollup client that classifying `/internal/analytics-rollup` did not repair. **`SEC-G5` closed 2026-08-06 at `98b479083`, on the delete-helpers exit.** The plaintext `export_to_tarball` / `import_from_tarball` helpers are gone; `flapjack::index::snapshot::{export_to_tarball, import_from_tarball}` no longer resolves, proven by a throwaway public-surface `E0432` unresolved-import probe alongside the focused `cargo test -p flapjack --lib --features s3-snapshots -- index::snapshot` pass. the private CONTROLS security record is the terminal owner and carries the machine-checkable `SEC-G5 disposition: closed-helpers-deleted` line. The `FLAPJACK_SNAPSHOT_KEY_FILE` / `FJSNAPE1` envelope remains the single snapshot-encryption scheme. Closing receipt: the reviewed private sec g5 terminal disposition receipt. **The row itself stays open** for the one remaining hygiene residual: a disposition for the production moderate advisories sitting below the audit gate's high-and-above threshold. | Public probes: `engine/tests/test_analytics_ip_e2e.rs`, `engine/tests/test_docker_runtime_e2e.sh`, `engine/dashboard/scripts/audit_gate_fixture_test.sh`, `engine/tests/resource_bounds_http_probe.sh`, `engine/tests/s3_sse_http_probe.sh`, `engine/tests/snapshot_encryption_replication_symmetry.sh`, `engine/tests/replication_peer_auth_http_probe.sh`; gap/control owner: the private CONTROLS security record; shipped S3 owner: `engine/src/index/s3.rs`; snapshot-byte owner: `engine/src/index/snapshot.rs`; receipts: the reviewed private s3 sse receipt, the reviewed private snapshot encryption receipt, the reviewed private replication peer auth receipt; remaining implementation owners: `engine/flapjack-replication/src/peer.rs`, `engine/dashboard/scripts/audit_gate.sh`; local tarball follow-up owner: `engine/src/index/snapshot.rs`. |
| GATE-1 | `main`'s security-audit gate is red, introduced by the `SEC-W2` TLS listener | **CLOSED 2026-08-02 by the `aug02_12pm_0_rustls_pemfile_advisory_remediation` lane.** `RUSTSEC-2025-0134` is resolved by removing the unmaintained `rustls-pemfile = "2"` production dependency from `engine/flapjack-http/Cargo.toml` and rewriting `engine/flapjack-http/src/tls_serve.rs::{load_certificates,load_private_key}` to parse PEM via `rustls::pki_types`, the re-export `rustls` 0.23 already provides (no dependency was added; `rustls` stays the single owner of the `rustls-pki-types` version). No ignore entry was added: `engine/.cargo/audit.toml` and `engine/deny.toml` stayed untouched — this is a real removal, not a suppressed advisory. Verified at the lane's refreshed `HEAD` (merge of `origin/main`): `cargo test --test test_security_audit -- a06_workspace_passes_cargo_audit_and_cargo_deny --exact` exits `0` (`1 passed; 0 failed`), and the non-vacuous check — restoring only the `rustls-pemfile` edge in a disposable worktree — returns the gate to red naming `RUSTSEC-2025-0134`. **History-safe closeout reconciliation `s40`:** after integrating current `origin/main`, `cargo check -p flapjack-http`, the final audit/deny/exact-`a06` block, both receipt checks, and the owned-file subset check all passed; this row and the receipt were then updated together in one forward commit, preserving immutable history. Receipt: the reviewed private rustls pemfile advisory receipt. Original filing retained below for provenance. **Open, filed 2026-08-02, measured at `fbe8207cb` rather than inherited from a receipt.** `timeout 900 cargo test -p flapjack --test test_security_audit -- a06_workspace_passes_cargo_audit_and_cargo_deny` returns `FAILED. 0 passed; 1 failed`, and `cargo audit --deny warnings` reports `error: 1 denied warning found!` for `RUSTSEC-2025-0134`, unmaintained `rustls-pemfile 2.2.0`. **The `aug02_7am_0` receipt attributed this to a pre-existing `origin/main` condition ("predates the auth lane"). That is true of the auth lane and false of the repository:** `git log -S"rustls-pemfile" -- engine/flapjack-http/Cargo.toml` returns exactly one commit, `091d622cd "Add static TLS listener support"` — the `SEC-W2` lane, merged the same night. `rustls-pemfile = "2"` is at `engine/flapjack-http/Cargo.toml:33`, a **production** dependency (not `[dev-dependencies]`), consumed by `engine/flapjack-http/src/tls_serve.rs:87,106`. So `SEC-W2` shipped a feature and a red security gate together, and the honest-but-narrow "not introduced by my lane" finding routed it to no one. This row is that owner. **It is not `SEC-QXML`** — different advisory, different crate, different upstream — and folding it into that row would hide it behind a blocked-on-upstream excuse it does not have. **Why the row exists at all:** a red merged-union gate every individual lane is entitled to skip is exactly how a repo arrives at a permanently red `main` with nobody accountable. | Owner: `engine/flapjack-http/Cargo.toml:33` and `engine/flapjack-http/src/tls_serve.rs`. Preferred exit is replacing the unmaintained crate — `rustls-pki-types` covers PEM parsing for `rustls` 0.23, so this is likely a small, local change in `tls_serve.rs::load_tls_config`, not a dependency stand-off. Fallback exit is an entry in `engine/.cargo/audit.toml` **and** `engine/deny.toml` (kept in lockstep, per `SEC-QXML` precedent) carrying a dated review condition; an ignore without one is a false close. Verify with `timeout 900 cargo test -p flapjack --test test_security_audit -- a06_workspace_passes_cargo_audit_and_cargo_deny` exiting `0`. **Measured not-a-defect on the same sweep:** the OpenAPI export drift the same receipt reported (`openapi_export_tests::committed_{docs2,demo}_openapi_matches_export_output`) no longer reproduces — `timeout 1800 cargo test -p flapjack-http --lib -- openapi_export_tests` passed `12 passed; 0 failed` at `fbe8207cb`. Do not re-file it. |
| SEC-G3 | Dashboard admin key readable by any same-origin script | **CLOSED 2026-08-03 at `bf29757dc`, on the falsifiable exit this row named.** The dashboard exchanges the admin key once for a server-owned `HttpOnly; SameSite=Strict` session cookie and persists no key material in the browser: `engine/dashboard/src/hooks/useAuth.ts` now `POST`s the key to `/1/dashboard/session` and its zustand `partialize` retains **only** `appId`, with a `migrate` that drops legacy persisted key material on upgrade. Authenticated reloads survive; `DELETE /1/dashboard/session` revokes server-side. Both named false closes were refused — this is neither `sessionStorage` nor in-memory-only. Fail-capability is proven at both tiers rather than asserted: with `HttpOnly` deliberately removed, `cargo test -p flapjack-http --lib -- auth::tests::session_transport_tests` went `4 passed / 4 failed` and the browser probe failed at `session_auth.spec.ts:44` on `expect(sessionCookie?.httpOnly).toBe(true)`; restored, engine passed `8/8` and browser `3/3`. The user contract is `docs/screen_specs/login.md`; receipt: `engine/docs2/4_EVIDENCE/2026_08_02_aug02_11am_8_dashboard_session_auth_receipt.md`. the private DECISIONS security record SD-009 is superseded by the shipped work, not merely revoked. **One defect this lane found and fixed rather than shipped past:** the first full-suite run went `162 passed / 146 failed` because direct cross-origin `/metrics` requests cannot carry an `HttpOnly` session under browser CORS rules; `b440f3028` routes development metrics same-origin through the Vite alias, and the committed-tree rerun went `330 passed / 4 failed`. Those four failures are classified, not hidden: two `migrate-algolia` cases reproduce identically on unmodified `origin/main` (external-credential condition) and two `analytics-deep` cases are a full-suite-only seeding collision (HTTP 500 at `analytics-seed.ts:87`) that passes in focused isolation — the latter is **not** closed and is routed to `JOIN-1`'s sweep. **Server-side half, shipped 2026-08-03 and retained here as the store contract.** `engine/flapjack-http/src/auth/session.rs` is the durable session-store owner: it mints, validates, revokes, and survives restart, and persists only `{"version":1,"key_fingerprint":"<hex>","verifiers":[{"salt":"<64-hex>","digest":"<hex>"}]}` to a `0o600` `dashboard_sessions.json`, where `key_fingerprint = hex(HMAC-SHA256(admin_key, "flapjack.dashboard_session_state.v1"))` and each verifier digest is a per-session-salted HMAC-SHA256. The plaintext token and the admin key each occur **zero** times in the persisted bytes, pinned by `persisted_state_leaks_neither_token_nor_admin_key`; exact key-set equality is pinned by `persisted_state_contains_only_keyed_session_verifiers`. `cargo test -p flapjack-http --lib -- session_store` reported `17 passed; 0 failed` against a Stage 1 red baseline of 14 named behavioral failures. Receipt: `engine/docs2/4_EVIDENCE/2026_08_03_aug03_5am_1_dashboard_session_store_foundation_receipt.md`. The route mount, cookie encoding, and exchange/revoke handlers that consume it are `engine/flapjack-http/src/{router.rs,auth/session_cookie.rs,auth/middleware.rs,handlers/dashboard_session.rs}`. **Original filing follows.** REOPENED 2026-08-02. Closed 2026-08-01 as accepted risk under the private DECISIONS security record SD-009; that acceptance is revoked because its only load-bearing premise — "the React directory is scheduled for deletion, not repair" — was reversed by operator decision. The console is now a maintained surface, so its credential handling has to be correct rather than survivable. **The defect:** the zustand `persist` store in `engine/dashboard/src/hooks/useAuth.ts` writes the API key — routinely the *admin* key — to `localStorage` under `flapjack-auth`, where any script in the page origin can read it. The shipped CSP (`script-src 'self'`, no `unsafe-inline`, no `unsafe-eval`) makes injection hard, but it is one mistake — one CDN script, one `dangerouslySetInnerHTML`, one dependency compromise — away from full admin compromise, and defense-in-depth is the whole point. **Reuse SD-009's analysis; do not re-derive it.** It already established that the correct fix is an httpOnly cookie session, that the engine has **zero** cookie/session machinery (verified 2026-08-01: no `set-cookie` / `httponly` anywhere in `engine/flapjack-http/src/`), and that fjcloud's `infra/api/auth/admin_session.rs` is Postgres-backed and therefore not portable to a single static binary. **Two named false closes, both of which must fail review:** `localStorage` → `sessionStorage` (same-origin script reads both identically — the gap is *read access*, not *persistence duration*), and in-memory-only storage (forces admin-key re-entry on every page load, which the control register's "preserving the dashboard authentication workflow" wording rules out). | Client owner: `engine/dashboard/src/hooks/useAuth.ts`. Engine owner: a new session seam in `engine/flapjack-http/src/auth/`, which must stay single-binary and dependency-light — the existing `${FLAPJACK_DATA_DIR}` file/sled precedent is the portable shape, not Postgres. Register: the private CONTROLS security record under Application security. Falsifiable exit: after login, `localStorage.getItem('flapjack-auth')` contains no key material and `document.cookie` does not expose the session (proving `HttpOnly`), asserted by a Playwright probe that fails when the flag is removed; the session cookie carries `HttpOnly`, `SameSite=Strict`, and `Secure` when served over TLS; an explicit logout revokes the session server-side, proven by replaying the cookie and getting `401`; and a page reload does **not** re-prompt for the admin key. |
| SEC-QXML | Remove quick-xml RUSTSEC ignores when rust-s3 updates | RUSTSEC-2026-0194/-0195 (quick-xml DoS, fix >=0.41) are temporarily ignored because rust-s3 0.37.2 (latest as of 2026-07-06) pins quick-xml ^0.38 — no upgrade path exists. When a rust-s3 release depends on quick-xml >=0.41: `cargo update rust-s3`, then delete the ignore entries from `engine/.cargo/audit.toml` and `engine/deny.toml` (kept in lockstep). The a06 nightly audit test enforces the rest. | Ignore owners: `engine/.cargo/audit.toml`, `engine/deny.toml`; gate: `engine/tests/test_security_audit.rs` (`a06_workspace_passes_cargo_audit_and_cargo_deny`); upstream watch: <https://crates.io/crates/rust-s3> |
| SEC-DNS1 | Outbound clients validate a resolved address and then re-resolve at connect time | **CLOSED 2026-08-05 at `9266acad3`, on exactly the exit this row named.** The single remaining unpinned caller is pinned: `AlgoliaClient::new` calls `pin_vetted_algolia_hosts` (`engine/flapjack-http/src/handlers/migration/algolia_client.rs:308`), whose body passes the vetted address vector to `reqwest::ClientBuilder::resolve_to_addrs` (`:518`). The row explicitly refused a test that only asserts the vet call happened, and the delivered proof honours that: it is a source-contract scanner carrying false-accept fixtures for substitution, a mutated `let mut` vector, reassignment, `Mutex`, `RwLock`, and `RefCell` — each of which was a real false-accept found and closed in sequence during the lane. Receipt: the reviewed private algolia connect pinning receipt; closeout: the private gate integrity and concurrency closeout note. **One residual survives closure and is a different defect, deliberately not left inside this row:** the scanner recognises a fixed list of interior-mutability wrappers rather than a rule, so a wrapper outside that list would false-accept again. That is scanner generality, not an unpinned caller, and it is queued in the private follow-ups note; do not reopen `SEC-DNS1` for it. Route this row to `implemented/` at the next history pass. **Original filing follows.** RE-MEASURED 2026-08-03 against the tree, and the filing below is substantially stale: the residual is ONE caller, not four.** The filing was copied from a follow-up row written 2026-05-27 and was not re-verified against current `main` before it became a ledger row — the same class of error as the `MUT_AGED.md` citation. Measured per caller at `40a115302`: `handlers/migration/typesense_client.rs:117-122,163-169,187` **pins**; `handlers/migration/meilisearch_client.rs:120-129,187-194,212` **pins**; `handlers/chat.rs:319-333` **pins** — it builds `pinned_connect_addrs` from the vetted resolution and `ai_provider.rs:185-187` feeds them to `resolve_to_addrs`, with the comment at `chat.rs:352` naming the TOCTOU close explicitly; `src/vector/embedder.rs` **pins** — `build_embedder_http_client` at `:611-630` calls `resolve_to_addrs` from `target.resolved_ips`; `src/vector/config.rs` is **not a client at all** and is correctly out of scope — it is the intake/disk-load validation gate, and its own doc comment at `:47-54` states that constructors do the pinning. **The one real residual is `handlers/migration/algolia_client.rs`:** `AlgoliaClient::new` at `:258-263` builds a `reqwest::Client` with connect/request timeouts, no-redirect, and `no_proxy()`, but **no `resolve_to_addrs`**, and the file's only `vet_outbound_url_target` call at `:556` is a test-only literal-loopback check that never reaches the production client. So an Algolia host that answers with a public address during any vetting and a private or loopback address at connect time reaches an internal target. **Falsifiable exit, unchanged in shape but now scoped to one caller:** `AlgoliaClient::new` passes the vetted `socket_addrs()` to `reqwest::ClientBuilder::resolve_to_addrs`, proven by a focused test in which a resolver returning a loopback address at connect time is refused. A test that only asserts the vet call happened does not close this row. **Original filing follows, retained because its analysis of the primitive and the reference implementations is still correct.** Filed 2026-08-03; the underlying defect was verified live and was 68 days old before it reached any ledger — it sat in the private followups note as `stage_01/s13_review_bugs_verified-dns-rebind-deferred` from 2026-05-27, where no planning pass could see it. Four of six `vet_outbound_url_target` callers vet the target's resolved address and then hand the raw URL to `reqwest`, which resolves again at connect time. A hostname that answers with a public address during validation and a private or loopback address at connect time therefore reaches an internal target. Affected: `engine/flapjack-http/src/handlers/chat.rs`, `engine/src/vector/embedder.rs`, `engine/src/vector/config.rs`, and `engine/flapjack-http/src/handlers/migration/algolia_client.rs` — the last of which the original follow-up never named, and which the 2026-08-03 audit found by enumerating callers rather than trusting the filing. | Fix primitive already exists: `engine/src/security.rs::VettedOutboundUrlTarget::socket_addrs()`, whose doc comment already instructs callers to pass the complete set to their HTTP client's resolver. Working reference implementations in-tree: `handlers/migration/typesense_client.rs:122,169` and `handlers/migration/meilisearch_client.rs:129,194`. Falsifiable exit: every caller of `vet_outbound_url_target` that builds an HTTP client also passes `socket_addrs()` to `reqwest::ClientBuilder::resolve_to_addrs`, proven by a focused test per caller in which a resolver returning a loopback address at connect time is refused. A test that only asserts the vet call happened does not close this row. Triage source: the reviewed private followup ledger triage and recurring blocker repair receipt (bare filename for traceability, unopenable on the mirror: `2026_08_03_followup_ledger_triage_and_recurring_blocker_repair.md`). |
| SDK-1 | The published Python, Ruby and Go SDKs send the caller's credentials to Algolia's production hosts, and the remediation is blocked on registry credentials that do not exist on this host | **OPEN, and DECIDED 2026-08-06 evening rather than left silent. Filed from `jul16_3pm_1_sdk_outbound_safety`, whose twelve `stage_05` rows are its handoff.** **The defect: the SDK rebrand is incomplete.** `search` resolves `*.flapjack.io` everywhere, but 34 non-search sources across five languages still resolve Algolia's production hosts while the transport attaches the caller's credentials as `x-algolia-*` headers. Verified in the **owner repos**, not only in `sdks/`: `flapjack-search-python` `flapjacksearch/analytics/config.py:66,68` resolves `analytics.algolia.com` with the key header at `:35`; `flapjack-search-go` `flapjack/ingestion/client.go:94` resolves `data.{region}.algolia.com`. Any non-search call discloses the caller's credentials to a third party. **Severity, honest rather than inflated: disclosure, not compromise.** Algolia rejects a Flapjack key, so nothing is accessed — but it lands in another vendor's logs and is burned. Exposure measured: PyPI `38` downloads last month, RubyGems `160` lifetime, and the project has no users. **Decision, made rather than deferred: WITHDRAW the published SDK line rather than patch it.** Patching ships corrected builds of a half-finished fork; these packages should not be published in this state at all. Withdrawal is yank/deprecate on PyPI and RubyGems plus a Go `retract`, then a GHSA on each owning repo — the advisory is owed *because* a yank cannot reach an already-pinned install. **Do not file advisories before the withdrawal**; one naming no remedy while the package stays installable is half an artifact. **Why it is not executed here, measured rather than assumed.** (1) *No registry credentials on this host* — probed, not inferred: no `~/.pypirc`, no `~/.gem/credentials`, no `TWINE_*`/`GEM_HOST_*` env, and `engine/.secret/` holds PEM material and an AWS CSV only. (2) *The owner repos must not be hand-edited* — the Python and Ruby ones carry `.github/workflows/do-not-edit-this-repository.yml`, which auto-closes PRs as generated content. (3) *Their `release.yml` is armed and unexercised* — it fires on a push to `main` whose message starts `chore: release` and publishes by OIDC, and **has never once run** (both historical runs conclude `skipped`), so `1.0.0` was published by hand with a token that is not here and the OIDC path is unproven. Editing those repos risks firing an unreviewed publish nobody has ever exercised. **This row is also the mechanism that stops the handoff being lost.** `drain_followups.py` expires a follow-up row on age unless a ledger names its lane; measured before this row existed, a drain at cutoff `2026-07-17` planned 26 closures with `jul16_3pm_1` appearing 16 times. Naming the lane here makes those rows unexpirable while this row stands. | Owners: `flapjackhq/flapjack-search-{python,ruby,go}`. In-repo guard `engine/tests/no_vendor_host_in_shipped_sdk.sh` is **GREEN** — zero executable vendor-host literals across all thirteen `sdks/` trees, so the source half is fixed and only the published artifacts are stale. **Cheapest first action when this unblocks: Go, which needs no registry credential** — a `v4.0.1` tag carrying `retract v4.0.0`, surfaced by both `go get` and `govulncheck`. PyPI and RubyGems need real tokens. **Probe, never recall — every figure above goes stale the moment anyone acts:** the PyPI and RubyGems version JSON for `yanked`, `proxy.golang.org/.../@v/list`, and `gh api /repos/flapjackhq/<repo>/security-advisories --jq 'length'` on all three. Falsifiable exit: both PyPI versions read `yanked=true`, the RubyGems version reads yanked, `@v/list` shows a Go version retracting `v4.0.0`, and each owning repo publishes one advisory — or a superseding decision to finish the rebrand is recorded here with the same probe evidence. |
| INFRA-1 | Unattended rented-box measurement harness | **SHIPPED 2026-08-02, filed here for the first time.** Three rows in this file already referenced `INFRA-1` as though it existed; it had no row anywhere, which made it unreachable by any planning pass. It exists because the old "measure only on a quiet host" standing order was unsatisfiable by construction — the fleet parallelism that made a measurement necessary was the same thing that kept the laptop busy — and it killed five lanes and turned `MIG-17` into a six-day park ending in ABANDON. `engine/loadtest/remote/run_remote.sh` launches, runs, captures evidence from, and destroys an ephemeral EC2 instance unattended. The acceptance suite drives a **real** launch and passed all four scenarios plus the terminal assertion: ordinary tagged launch with propagated failure and verified evidence; interrupt teardown leaving zero live instances; orphan sweep that spared controls; and a zero-budget refusal that declined to launch before spending anything (`0.000289 USD at 0.0104 USD/hour`). `PREFLIGHT_EXIT=0`, `GREEN_EXIT=0`, account `213880904778`, `us-east-1`. Already consumed in anger by two downstream lanes: `aug02_11am_6` ran the dashboard composition twice on `i4i.2xlarge` for `0.31` USD total. **Renting a measurement box is not a deployment** — no sync, no release, no registry, no DNS, no persistent cloud state. | Owner: `engine/loadtest/remote/run_remote.sh` and `engine/loadtest/remote/lib`; behavioral oracle: `engine/loadtest/tests/remote_runner_acceptance.sh` (a mocked teardown does not satisfy it — the interrupt scenario must assert `aws ec2 describe-instances` reports zero non-terminated instances for the run's unique tag); lifecycle policy: `engine/loadtest/AWS_SCALE_CEILING_RUNBOOK.md`; receipt: the reviewed private remote measurement harness receipt (bare filename for traceability, unopenable on the mirror: `2026_08_02_aug02_11am_1_remote_measurement_harness_receipt.md`). **Citation repaired 2026-08-03:** this cell previously named a receipt called `MUT_AGED.md` in the evidence tree — a file that has never existed in this repository. (Its full path is deliberately not written out here: `engine/tests/ledger_correction_gate_test.rs` now requires every evidence path a ledger names to resolve, and it cannot distinguish a citation from prose quoting a bad citation. To discuss unopenable evidence, name the bare filename, as this sentence does.) That was not cosmetic — `engine/tests/ledger_correction_gate_test.rs` matches a receipt's *file name* against the ledger text, so the real receipt read as uncited, aged past `GRACE_HOURS = 24` at 2026-08-03T16:41Z, and turned `cargo test --workspace` — this repository's wave-transition gate — red for every live batch. The harness was never the defect; the citation was. Credentials verified working 2026-08-02 and again 2026-08-03; no lane may treat AWS access as an unknown. |
| BUILD-1 | Shared-host build capacity and warm-gate measurement | **CLOSED 2026-08-04.** Host build capacity reclaim is evidenced by the reviewed private build capacity receipt. The safe owner reclaimed exactly 542,100,000 bytes (411,500,000 builder cache plus 130,600,000 unreferenced volumes), preserved images, worktree directories, and `engine/target`, and left a tested fail-closed rerunnable command. The original 40 GB threshold was not met because the historical cache was already absent; the receipt records the 39,457,900,000-byte shortfall and retained named-volume boundary. The accepted sequential workspace `cargo test --no-run --workspace` comparison at source SHA `8c994fb225cf0fc30b83d2a39d9143083227834a` used fresh top-level durable outputs under `${MATT_HOME:-$HOME/.matt}/evidence/aug03_11am_2b_build_capacity_warm_measurement_completion/`: WARM1 completed in `108.350s` from load averages `41.02 19.78 14.77` to `21.96 19.33 15.19` with `OVERLAP_SAMPLE_COUNT=10` and `OVERLAP_VERDICT=ACCEPTED`; WARM2 completed in `127.777s` from `19.89 19.26 15.53` to `15.30 16.91 15.05` with `OVERLAP_SAMPLE_COUNT=12` and `OVERLAP_VERDICT=ACCEPTED`. Both runs exited `0` with block exits `0`; first-sample, maximum-sample, and completion cadence gaps were `0s` / `12s` / `9s` for WARM1 and `0s` / `12s` / `8s` for WARM2. WARM2 took `19.427s` longer, `17.93%` longer, and `1.179x` as much wall time as WARM1. | Reclaim owners: `engine/_dev/s/reclaim_build_cache.sh` and `engine/_dev/s/tests/reclaim_build_cache_acceptance.sh`; warm evidence owner: `${MATT_HOME:-$HOME/.matt}/evidence/aug03_11am_2b_build_capacity_warm_measurement_completion/`; receipt: the reviewed private build capacity receipt. Falsifiable regression: rerun the safe reclaim acceptance suite and two sequential bounded same-locality `cargo test --no-run --workspace` specimens, requiring command/block exits `0`, recorded wall times, endpoint loads, and no foreign toolchain overlap. Do not adopt a shared `CARGO_TARGET_DIR`; its lock would serialize concurrent worktrees. |
| TEST-HANG-1 | `flapjack-http` test binary stalls, blocking wave transitions | **CLOSED 2026-08-03 at `40b5acc41`, on the exit this row named.** Root cause: `crate::test_helpers::ENV_MUTEX` is a non-reentrant `std::sync::Mutex` and the single owner of process-environment mutation. Tests hold it across `.await` deliberately, so production code reached by the request under test runs while it is held; the Algolia base-URL reader on that path had a "does this thread hold it?" escape hatch, but the flag it consulted was maintained by exactly **one** acquisition path — a private thread-local next to one guard type. The ~60 tests that call `ENV_MUTEX.lock()` directly left it `false`, so `read_override` re-locked and **self-deadlocked**. Proved by reproducing at `--test-threads=1`: single-threaded, no other thread can hold the lock, so the blocked thread was blocking on a lock it already held. `sample` showed `read_override -> __psynch_mutexwait`. **Fix:** the flag now belongs to the mutex — `EnvMutex::{lock,try_lock}` maintain a crate-level thread-local holder flag, so every existing call site became correct without being edited, and the duplicate thread-local was deleted. Re-entry panics with a named message instead of hanging, and poison is recovered inside `EnvMutex` (the red run exposed that one panic while holding would otherwise cascade through every `.expect(..)` site and bury the original failure). **Evidence:** before — infinite hang, 0.0% CPU, 8m20s, no output. After — `2300 passed; 0 failed` in `127.43s` at the same `--test-threads=4`, and the four previously-stalled tests pass in `0.05s`. Three TDD contract tests in `test_helpers` (2 of 3 red on their own assertions before the fix), plus a strengthened migration SSOT guard that rejects any private holder flag, mutation-probed red. `cargo fmt --check` clean. **The gate itself is the closing proof:** the identical command the 2026-08-03 14:24Z supervisor tick ran and killed at `WORKSPACE_EXIT=143` — `(cd engine && timeout 3000 cargo test --workspace)` — now returns **`WORKSPACE_EXIT=0`**, across 83 test binaries and 5,781 tests with zero `test result: FAILED`. **Superseded lane:** the private test env isolation hang repair note was authored against this row's original text and is no longer needed for the deadlock. **What is NOT closed here** is the separate orphaned-process leak described below — that is `TEST-ORPHAN-1`. Route this row to the private implementation history at the next history pass. **Original filing follows.** Open, filed 2026-08-03. The 2026-08-03 14:24Z supervisor tick ran the declared source-parity Wave-2 transition command verbatim — `(cd engine && timeout 3600 cargo test --workspace)` — and the `flapjack_http` test binary reached a state with numerous tests over 60 seconds, **0% process CPU**, and no log progress for nearly four minutes. `WORKSPACE_EXIT=143` (killed). `FJ-5` was correctly not launched through an indeterminate gate, so this stall is what is holding the `aug02_5am_0` critical path right now — and that path is not local: `fjcloud`'s M3b → M4 → C0 chain has been parked since 2026-07-28 behind it. 0% CPU with no progress is a **deadlock or blocked await**, not slowness, so it will not resolve by raising the timeout. **Reproduced and stack-sampled 2026-08-03 15:45Z on the canonical clone at `3d0d2a7a4`.** `timeout 1500 cargo test -p flapjack-http --lib --no-fail-fast -- --test-threads=4` wedged: the test binary sat at 0.0% CPU for 8m20s and produced no output. `sample <pid> 5` shows the harness main thread parked in `test::run_tests_console` → `mpmc::recv` → `thread::park` (waiting on a test that never completes), while several current-thread tokio runtimes are parked in `mio::poll::Poll::poll` — blocked awaits, not slowness. The stalled frames name four tests, all in `router_tests`: `list_source_indexes_gates_loopback_behind_opt_in`, `internal_snapshot_capability_requires_admin_authentication`, `internal_snapshot_capability_remains_available_without_authentication`, and `algolia_list_indexes_compat_contract_is_preserved`, with `handlers::migration::algolia_client::test_algolia_base_url_override` and `test_algolia_base_url_env::read_override` on the same stacks. **Leading hypothesis, evidence-backed and cheap to falsify:** `crate::test_helpers::ENV_MUTEX` is a process-global mutex that `AlgoliaBaseUrlEnvGuard::{vendor_hosts,overridden_to}` and `EnvVarRestoreGuard` hold **for the whole test body, across `.await` boundaries** (`router_tests.rs:1263` and `:2034` hold it across wiremock-served requests). Any holder that blocks on I/O serializes and then wedges the entire binary behind it. *(Corrected 2026-08-03 — the original filing added here that `AlgoliaBaseUrlEnvGuard::try_overridden_to` "falls back to proceeding without the lock", implying an unguarded mutation path. That was wrong and is deleted rather than retained: that function has exactly one caller, the guard's own contention self-test, which asserts on `.is_some()`. It was never a production hazard and was not part of the defect.)* The `aug02_5am_4` lane visibly fought this class in the hours before the stall — `3be8704cf` *Share Algolia test base URL env constant*, `16d7d9c98` *serialize Algolia base URL overrides*, `1a51225a1` *harden Algolia override isolation*, `e8d840e32` *simplify guarded Algolia request helper* — which is a strong signal the isolation is partial rather than wrong-in-principle. **Related leak, same class, independently confirmed:** three orphaned `flapjack_http` test processes (PIDs `7500`, `7823`, `18408`, all `PPID=1`, state `UE`) have been stuck for **1d22h** running `handlers::migration::preview_tests::meilisearch_live_preview_reports_exact_seeded_counts_and_codes --ignored --exact`, from the already-merged `aug01_9am_1` worktree. A merged lane is still leaking uninterruptible processes onto the shared host two days later; whatever reaps test children does not cover this path. | Exit: name the specific test(s) that stall, with a reproduction at a bounded `--test-threads` count, and either stop holding `ENV_MUTEX` across `.await` (take the value under the lock, drop the guard, then await) or assert the setup precondition before the await — per the standing convention that an async test awaiting an external service must assert the setup precondition succeeded **before** awaiting the operation. Diagnostic entry point: `timeout 1500 cargo test -p flapjack-http --lib --no-fail-fast -- --test-threads=4`, then bisect by module. Do not "fix" this by extending the timeout or by marking tests `#[ignore]`; a wave-transition gate that cannot distinguish hung from slow is not a gate. See also `PL-2`, which is the scheduled re-evaluation of `cargo-nextest` per-test timeouts and whose trigger condition this row is evidence for. |

| TEST-ORPHAN-1 | A killed `cargo test` leaves unkillable orphaned test binaries on the host | **Open, filed 2026-08-03. Split out of `TEST-HANG-1`, whose deadlock half is closed; this is the separate leak the same investigation surfaced.** Three `flapjack_http` test binaries from the **already-merged** `aug01_9am_1` lane were found running `handlers::migration::preview_tests::meilisearch_live_preview_reports_exact_seeded_counts_and_codes --ignored --exact --nocapture`, `PPID=1`, state `UE`, elapsed **1 day 22 hours**, and **immune to `SIGKILL`** (verified: `kill -9` on all three left all three running). **Re-measured 2026-08-03T20:5xZ: the same three PIDs (`7500`, `7823`, `18408`) are still `PPID=1`, still state `UE`, now at `2 days 8 hours`.** They are running `handlers::migration::preview_tests::meilisearch_live_preview_reports_exact_seeded_counts_and_codes` out of a worktree for `aug01_9am_1`, a lane that merged on 2026-08-01 — so the leak outlives the lane, the branch, and the merge, and elapsed time is still climbing. That is the row's claim confirmed rather than restated: nothing reclaims these short of a reboot. `lsof` shows fds 1 and 2 are pipes with no live reader. **Mechanism:** `timeout` bounds `cargo`, not the test binary `cargo` spawned — when the bound fires, the test binary is re-parented to `init` and keeps running; Rust's std ignores `SIGPIPE`, so writing to a pipe whose reader has died returns `EPIPE` rather than terminating the process, and `--nocapture` writes test output straight into that pipe. A process already blocked in an uninterruptible syscall cannot then be killed at all. **This is caused by the repo's own documented invocation idiom** (`timeout N cargo test ... 2>&1 \| tail -N`), which is why the fix is partly a rules change, already landed at `40b5acc41` in `.scrai/rules.md`: bound runs write to a file, and any bounded run that was killed must be followed by a `PPID=1` orphan sweep. **What remains:** the live-service test itself is unbounded — `preview_tests.rs` holds `ENV_MUTEX` via `with_env_var` and then awaits a served request against a live Meilisearch with no in-test deadline, so an unreachable upstream wedges it indefinitely. Wrap the awaited request in `tokio::time::timeout` with a named failure. **Deliberately not done in this pass: `engine/flapjack-http/src/handlers/migration/**` is owned by the live `FJ-5` lane and editing it would collide.** Falsifiable exit: kill a bounded run of that test mid-flight and show zero `PPID=1` `flapjack_http` processes survive, plus a focused test proving the in-test deadline fires. **Measured cost of the three surviving wedged processes, so nobody spends a lane chasing them:** `14400` bytes RSS each (~43 KB total), `0.0%` CPU, and **zero network sockets or ports held** (`lsof -p <pid> -a -i` is empty for all three). They are inert residue, not a live problem; they cannot be reaped and clear only on reboot. **The cost that mattered was invisibility, not resources** — five orphans accumulated across two merged lanes over two days and were found only by accident while chasing an unrelated hang. That half is now closed: `matt.host_health.find_orphaned_test_processes` (mike_dev `matt_root/matt/host_health.py`) reports any `PPID=1` process living under `target/<profile>/deps/` as a **warning** finding on every fleet health check, and is proven non-vacuous against this host — it returns exactly these three and no false positives, with five live lanes' test binaries and every system daemon correctly excluded. Severity is capped at warning by a dedicated test, because a cosmetic orphan must never be able to halt fleet startup. | Owners: `engine/flapjack-http/src/handlers/migration/preview_tests.rs` (after `FJ-5` merges), `engine/tests/meilisearch_source_contract_kat.sh`, and the landed test-discipline rules in `.scrai/rules.md`. See also `PL-2`: this row plus `TEST-HANG-1` are direct evidence for that row's `cargo-nextest` per-test-timeout trigger condition, ahead of its 2026-11-26 review date. |

| DOC-LAYOUT-1 | `ROADMAP.md` and `PROJECT_OVERVIEW.md` exceed their doc-layout caps, almost entirely from retained prior-revision text | **CLOSED 2026-08-03, on both halves of the exit this row named.** `matt scrai assemble` reported `ROADMAP.md exceeds 200-line v2 cap (502 lines)` and `PROJECT_OVERVIEW.md exceeds 120-line v2 cap (139 lines)` at filing, then **`548` and `156`** after the same-day catch-up — the files grew even though that pass superseded rows in place, because growth was dominated by the retained revision blocks already present, not by new edits. Compaction ran the same day: `ROADMAP.md` **548 → 156** (the five-deep revision stack deleted, plus five closed rows removed from the `Up Next` dispatch table, where listing closed work is the same defect as a retained revision) and `PROJECT_OVERVIEW.md` **156 → 118** (each priority reduced to statement, rank rationale, and a pointer, with evidence detail left to its owning row). Both exits met: `matt scrai assemble` now emits **no cap warning for either file**, and the row-ID set is **byte-identical before and after** — 44 first-cell values across `Active`, `Up Next`, and `Planned`, compared with `diff` rather than by eye, because a size reduction that loses a row is a regression and not a fix. History was routed nowhere: `git log -p` already holds it. This is filed as a row rather than left as a tool warning because a warning nobody owns is re-emitted forever and acted on never — which is the same failure this file has already recorded twice for the "Wave 0" capacity paragraph. **Cause, not merely size:** this ledger revises by prepending the new state and keeping "Prior revision follows verbatim", five deep. That makes it grow monotonically, so its tail is stale by default and can quietly contradict its head — the exact defect the 2026-08-03 fleet doctrine names in `~/.matt/scrai/globals/standards/doc_ssot_discipline.md` under "The Ledger Is Written, Not Owed". Supersede in place and let history live in git; keep provenance by naming the commit or receipt that changed a claim, not by retaining the claim. **Deliberately not done in this session:** the compaction is a large diff to a file that two live lanes and an active supervisor read and write, and it was not worth a conflict at closeout. | Owner: this file plus `PROJECT_OVERVIEW.md`. Falsifiable exit: `matt scrai assemble <repo>` emits no cap warning for either file, **and** a diff review shows every `Active`/`Up Next`/`Planned` row ID present before the compaction is still present after it — size reduction that loses a row is a regression, not a fix. Route the deleted revision prose nowhere: `git log -p` already has it. |
| DOC-PUBLIC-1 | Publicly-synced ledgers cite evidence the public mirror does not receive | **Open, filed 2026-08-03.** `.debbie.toml` syncs `README.md`, `PROJECT_OVERVIEW.md`, `ROADMAP.md`, `CHANGELOG.md`, and `engine/docs2/FEATURES.md` to the staging and prod mirrors, but `engine/docs2/4_EVIDENCE/` is an **individual-file allowlist** and `docs/`, `chats/`, and `implemented/` do not sync at all. Measured 2026-08-03 after the `DOC-LAYOUT-1` compaction: **38 receipt citations in publicly-synced ledgers name a receipt that is not on the allowlist** (`ROADMAP.md` 25, `engine/docs2/FEATURES.md` 13), plus 8 `chats/` and 11 `docs/` references. On the mirror each of those is a claim whose proof a reader cannot open — the same defect class as `INFRA-1`'s fabricated citation, except caused by sync scope rather than by a typo, and invisible in the dev repo because every path resolves here. `engine/tests/validate_doc_links.sh` cannot catch it: it resolves links against the **dev** tree. The new citation-existence assertion in `engine/tests/ledger_correction_gate_test.rs` cannot either, for the same reason. **This is a judgement call per reference, not a mechanical fix** — some receipts should join the allowlist, some citations should become prose that names the evidence without implying a resolvable path, and `chats/` paths should simply not appear (`.debbie.toml` already says internal chats paths are to be kept out of public sync). Do not "fix" it by bulk-adding receipts to the sync allowlist; several contain competitive research and internal orchestration detail. | Sync-scope owner: `.debbie.toml`. Falsifiable exit: a check that resolves every path a publicly-synced ledger names **against the set `.debbie.toml` actually publishes**, not against the dev tree, and reports zero unresolvable references — with a named live specimen proving it fails today at 38. Until that exists, the count above is the measurement and it was taken by hand. |

## Up Next

Dispatch sequencing for the next batch. **This section owns ordering and
parallelism only** — every row's scope, evidence, and owners stay in `Active` or
`Planned`. Reference by ID here; do not restate a row's content.

**Current prerequisites, refreshed 2026-08-06 evening.** No lane is barred from
`engine/`, `.github/workflows/release.yml`, or `CHANGELOG.md` (`REL-11` closed), and
warm compile measurement is not a dispatch blocker (`BUILD-1` accepted). **One lane
is live** — `aug06_10am_1a`, owning
`engine/flapjack-http/src/handlers/migration/{mod.rs,meilisearch_client*,typesense_client*}`
and `engine/tests/migration_release_loopback_contract.sh`. Nothing else may be
dispatched against those paths, and `MIG-21`'s dry-run lane is gated behind its merge.

**Prove file-disjointness by reading the tree, not the row titles.** On 2026-08-06 this table's predecessor declared `MIG-21`'s console lane and `NIGHT-1`'s dashboard-harness
lane disjoint. They were not: both edited `engine/dashboard/scripts/playwright-webserver.mjs` and its test, merged three hours apart, and left `npm run test:unit:run` red on
`main` for ~4h with two semantic conflicts git could not see — an invalidated spy-arity assertion, and an injected parameter the other lane had renamed, which fell through to
the real readiness probe and opened a live socket from a unit test. Repaired at `f6fcf4773`. **A disjointness claim must be produced by `git log --since=<batch start> -- <path>`
over every owner path in the batch, not asserted from the lane charters.**

**Standing rule: the ledger writer is never a terminal lane.** Every batch
through 2026-08-02 assigned its `ROADMAP.md` / `PROJECT_OVERVIEW.md` /
`engine/docs2/FEATURES.md` / `CHANGELOG.md` edits to a lane at the *end* of its
wave graph, which makes the writer a dependent of the lanes it describes. When
the batch stalls above it, the ledger never gets written — it has now failed
this way at least four times, and the fourth (2026-08-03) also turned `main`
red. The single-writer invariant stays; the position changes. A batch's ledger
writer is Wave 1 of the *following* batch. **And a correction is discharged at
the merge that lands its receipt, not at batch close** — the verbatim text
already exists at that point and a supervisor is already present.
`engine/tests/ledger_correction_gate_test.rs` bounds how long a correction may
sit unabsorbed and checks that every evidence path a ledger names can be opened;
it cannot schedule the writer.

**Host build capacity is a tracked standing precondition.** It gates every wave
transition in this repo and is owned by row `BUILD-1`; use that row and its named
measurement follow-up instead of re-filing an unowned "Wave 0" slogan.

| ID | Why now |
|----|---------|
| `MIG-22` | **The single highest-leverage row in this file, because it is what stands between a landed capability and a proven one.** `MIG-21`'s console half is merged and its two provider specs are red for exactly one reason: the release binary the e2e harness builds compiles the loopback admission seams out. Until this closes, no browser proof exists for two of the three providers, `JOIN-1` cannot reach its predicate, and the screen contract must keep describing the flow as unproven. Already in flight in lane `aug06_10am_1a`; nothing else should be dispatched against those two backend files. |
| `MIG-21` | **Still the batch's product headline, and now half-done.** The provider half landed 2026-08-06; the **dry-run half is unstarted** and is the capability neither competitor's console has. Its lane file is authored as the private `aug06_10am_7` console-migration dry-run checklist and gated on `MIG-22`'s lane merging — dispatch it the moment that gate clears, not at the next batch boundary. |
| `NIGHT-1` | **Highest-priority signal row, and for the first time it is a wait rather than a repair.** Three of four attributed buckets now carry landed repairs and 2026-08-07 is the first scheduled run that can exercise any of them. The exit is two consecutive green scheduled runs, so the earliest possible closure is 2026-08-08 — which is a reason to dispatch the Clippy-bucket investigation now, not to idle. |
| `TEST-FLAKE-2` | Dispatch **before** re-attempting `TEST-FLAKE-1`'s broad gate, not after. It is uncensused, so no existing disposition covers it, and it is the reason the last full-lib run could not exit `0`. The wave-transition gate in this repo is `cargo test --workspace`; an unattributed red there taxes every lane in the fleet. |
| `TEST-FLAKE-1` | Narrowed, not closed. The signal-based wait landed and the target passed its detached specimen; what remains is one broad gate that exits `0` rather than `124`. Sequence it behind `TEST-FLAKE-2` so the broad run has a chance of being clean. |
| `CI-E2E-1` | Repair landed and hardened; the only remaining step is **observing** the job green on the prod mirror at a SHA where it previously failed. That is a mirror-sync-and-watch task, file-disjoint from every other row here, and it does not need a code lane. |
| `TEST-ORPHAN-1` | **Dispatch window is open now and was not before.** It was deliberately held out of the `aug06_10am` batch because its process-group fix changes how every lane in the fleet runs `cargo test` and six lanes were in flight. One lane is in flight now. The leak has been re-confirmed twice, is immune to `SIGKILL`, and `aug06_10am_4` added a fresh instance — a 13 GiB detached worktree left registered after its cleanup guard wrongly reported the path already absent. |

Closed rows were removed from this table on 2026-08-03 (`DUR-2`, `MIG-12`,
`MIG-16`, `MIG-17R`, `GATE-1`), on 2026-08-04 (`SEC-W2A`, `DUR-1`), and on
2026-08-06 (`MIG-15`, `MIG-20`, `DUR-3`, `REL-11`, `MIG-18`, `SEC-G5`, `REL-12`, `PUB-GUARD-1`, `PL-15`). `PL-15` closed with three per-caller reverted-red proofs in the reviewed private loadtest listener-ownership receipt.
`MIG-15` and `MIG-20` closed with the release-profile
CLI-to-server-to-live-Meilisearch proof in
`engine/docs2/4_EVIDENCE/2026_08_05_aug05_12pm_1_migration_preview_served_cli_meilisearch_receipt.md`;
`DUR-3` closed with the canonical writer and snapshot-exclusion proof in
the reviewed private durable writer snapshot exclusion receipt. `REL-11`
closed with the published `v1.0.11` customer channel (release run `31096713795`);
`MIG-18` closed at `52ad0fdc7` with the served over-cap `413` proof in
the reviewed private bulk upload bound and pin scanner rule receipt;
`SEC-G5` closed at `98b479083` with
the reviewed private sec g5 terminal disposition receipt.
`REL-12` closed with the `release_ci_status_preflight` job that the `release` job now
waits on, and `PUB-GUARD-1` with the guard `.debbie/post-sync.sh` sources before it
mutates the mirror; both carry behavioural contracts rather than shape greps.
A dispatch table listing closed work is the same defect as a retained revision
stack; their state lives in git and the cited receipts.
`engine/tests/ledger_closed_row_consistency_test.rs` now enforces that, here and
in `PROJECT_OVERVIEW.md`'s priority list.

**`jul30_12am_0` is still registered in the private supervise ledger and is not closed.**
`closeout_artifact_check.py --roster` reports five unaccounted lanes
(`jul30_12am_1` … `jul30_12am_5`), none of which carries a `Disposition:` line
or a surviving workspace.

**Not up next, and why.** `PR-10` no longer waits on `DUR-1`; its remaining open
condition is the originally planned chaos-test mode that was never authored, so
it is not a current `ROADMAP.md` dispatch candidate. `jul30_12am_6`'s repaired-ladder
re-measure was **stopped without a verdict** and salvaged into
[`engine/docs2/4_EVIDENCE/2026_08_02_aug02_5am_1_ladder_salvage_and_gate_disposition.md`](engine/docs2/4_EVIDENCE/2026_08_02_aug02_5am_1_ladder_salvage_and_gate_disposition.md);
that lane's parked 1,000,000-record proxy was captured under fleet load, is
classified there as `not a capacity claim`, and is therefore not published as a
capacity or throughput number. **The single-machine scale
record stays uncorrected, and the reason has changed — do not inherit the old
one.** "Needs a quiet host" is void: measurement needing an idle machine routes
to a rented box under `INFRA-1`, and that harness demonstrably works
(`aug02_11am_6` used it twice for real dashboard suites). `aug02_11am_7` then
took three authorized attempts at the reference-scale ladder **on rented
hardware** and produced **no completed rung** — one stopped during setup, one
during runner bootstrap, and one proved reference locality then failed ahead of
rung 1. `engine/loadtest/BENCHMARKS.md` was deliberately left unchanged, and no
bootstrap failure, setup failure, locality-only result, capacity preflight, or
local-laptop proxy was promoted into a scale number; that restraint is correct
and must be preserved. **The residual is harness reliability on remote, not host
contention.** Counting the five lanes the old quiet-host predicate killed, this
measurement has failed or parked across at least eight attempts over nine days,
so the next lane must be scoped to *make the ladder survive one rung on a rented
box*, not to publish a curve. Command shape: `bash engine/loadtest/scale_ladder.sh
--profile standard --rungs <list> --bulk-build-throughput-probe --batch-size 10000
--stop-after-rung <n> --server-binary <path> --data-dir <fresh> --results-dir
<fresh>`, with a fresh scratch `--data-dir` outside the repo tree. `PL-10` needs a
new falsifiable question beyond the verified 1M floor, not another run.
`ADR-0005 OQ4` is a resolved v1.1 design awaiting a cross-node execution
decision. `SEC-QXML` is blocked on an upstream `rust-s3` release. `DOCS-CLAIMS`
and `PL-11` live on surfaces this repo does not own.

## Planned

| ID | Work Item | Planned Direction | Evidence / Owner |
|----|-----------|-------------------|------------------|
| globals-mandate-doc-paths-the-structural-gate-refuses-aug03 | Keep globally mandated documentation paths aligned with the structural merge gate. | Add a non-vacuous contract test that checks globals-mandated `docs/<child>/` paths against the canonical allowlist, then remove shadowed repo-local `docs/screen_specs/` exceptions. | Source: the private pm flapjack dev  canonical global mandates a docs path the merge gate refuses note. Exit: `python3 -m pytest matt_root/tests/test_scrai_assemble_v2_structural.py -k globals_mandated_docs_children -q` passes and `grep -c 'docs/screen_specs/' fjcloud_dev/.scrai/allowed_top_docs.txt mike_dev/.scrai/allowed_top_docs.txt` prints `0` for both files. |
| MIG-12 | Provider-neutral source-migration core | **CLOSED 2026-08-02.** One neutral internal adapter/capture seam feeds one shared spool, job runner, status/cancel/ACK, and fenced-publication lifecycle for the three public provider route families. Algolia compatibility aliases and Algolia-only resume remain intact; recurring connectors and arbitrary source families remain separate. | Closing receipt: the reviewed private mig12 provider neutral core receipt. `MIG-13` and `MIG-14` remote enablement remain unshipped. |
| MIG-13 | Meilisearch source migration | **Code-verified locally for the shared source-migration lifecycle and landed-data fidelity.** The served provider-parity probe boots a real digest-pinned Meilisearch container and a real auth-enabled `flapjack-server`, submits `configured_pk`, and proves two searchable documents with exact `objectID`/`sku`/`title`/`price`/`stock` values. The declared primary key `sku` maps to `objectID`, asserted by `LANDED_DATA meilisearch=PASS configured_pk=2 identity=sku_to_objectID`. Remote enablement remains default-off and later; this proof does not claim settings, synonyms, curation/rules, permissions, resume, or HA coverage beyond the focused ranking-rule contracts. | Receipt: `engine/docs2/4_EVIDENCE/2026_08_03_aug03_11am_5_competitor_migration_lands_data_receipt.md`; landed merge: `2c05776c7b9d8f60bae89c34ad819ece084fa2e4`; prior adapter receipt: the reviewed private m2em meilisearch adapter receipt; probe owner: `engine/tests/source_migration_provider_parity_http_probe.sh`. |
| MIG-14 | Typesense source migration | **Code-verified locally for the shared source-migration lifecycle and landed-data fidelity.** The served provider-parity probe boots a real digest-pinned Typesense 30.2 container and a real auth-enabled `flapjack-server`, submits the categories and products collections, and proves one category plus two products are searchable with exact projected values. Document `id` maps to `objectID`, asserted by `LANDED_DATA typesense=PASS categories=1 products=2 identity=id_to_objectID`. Remote enablement remains default-off and later; this proof does not claim settings, synonyms, curation/rules, permissions, resume, or HA coverage beyond the existing typed schema/export attribution contracts. | Receipt: `engine/docs2/4_EVIDENCE/2026_08_03_aug03_11am_5_competitor_migration_lands_data_receipt.md`; landed merge: `2c05776c7b9d8f60bae89c34ad819ece084fa2e4`; prior adapter receipt: the reviewed private m2et typesense adapter receipt; probe owner: `engine/tests/source_migration_provider_parity_http_probe.sh`. |
| MIG-16 | `flapjack migrate` CLI provider parity | **CLOSED 2026-08-02 — the served half went green on its own falsifiable exit.** The `aug02_5am_2` lane (`FJ-2`) root-caused the four `real_server_` startup-banner timeouts to a leaked auto-port child process and fixed it at `f24cdb9ca` (*Reap auto-port child on startup failure*). The row's exit command then reported `test result: ok. 4 passed; 0 failed` in three separate processes, recorded at `abe7f2647` with the one-minute load average for each: `60.48`, `52.11`, and — the important one — a re-run at Stage 1's red locality with `--test-threads=1` at load `48.26`. The unfiltered binary passed 37/37 and the whole `flapjack-server` crate gate exited `0`. Provider coverage was already proven (`MIG16_PROD_EXIT=0`, nine production hits; `MIG16_TEST_EXIT=0`, 42 CLI-test hits). Recorded nuance: the checklist asked for three filtered runs *plus* one at the red thread count and got two filtered plus the red-locality run — the red-locality run is the stronger evidence, so this closes, but it is not the literal 4-run shape. Route this row to the private implementation history at the next history pass. | Owners: `engine/flapjack-server/src/migrate.rs`, `engine/flapjack-server/tests/migrate_cli_test.rs`; provider enum: `AsyncMigrationSourceProvider::PUBLIC`. Closing evidence: the private migrate cli served probe isolation note closeout section. Re-falsify with `timeout 600 cargo test -p flapjack-server --test migrate_cli_test -- real_server_`; anything other than `4 passed; 0 failed` reopens the row. |
| MIG-17 | Migration-suite runtime-margin verifier is built and stranded on an unmerged branch | **CLOSED 2026-08-02 as ABANDON — the third of the three permitted outcomes.** The reviewed private migration-runtime-margin abandonment receipt is on `main` and names the branch and tip `c2ca0c3ea`, satisfying this row's stated closing condition. Reason, measured rather than asserted: the stranded verifier pins a normalized 161-scenario registry hash `0b81476d…`, while the current owner `engine/tests/migration_import_contract_test.sh` has 162 scenarios and hash `d892b59c…` (the extra scenario is `selftest_failure_evidence_retained`), so its `≥15%` median-improvement and `≤360s` assertions no longer describe the current denominator. Its profile and scheduler machinery is absent from `main` and no preserved evidence exists to re-derive the thresholds honestly. **What this closure does NOT do:** the underlying property is still unverified — `main` has no migration-import meta-suite runtime-margin evidence at all. That residual gap is `MIG-17R` below, not this row. Route this row to the private implementation history at the next history pass. **Original filing follows.** `origin/batman/jul26_am_11_migration_suite_runtime_margin_continuation` is 29 commits ahead of `main`, last touched 2026-07-26, and carries `engine/tests/verify_migration_import_runtime_margin.py` (840 lines, **absent from `main`**), a 1,371-line hardening of `engine/tests/migration_import_contract_test.sh`, and an evidence note named `2026_07_26_migration_import_meta_suite_runtime_margin_gap.md` (on that branch only — it was never merged, so no path in this tree resolves to it). Verified 2026-08-01: no successor implements this on `main` — `git ls-tree -r origin/main` matches `runtime_margin` only in the two lane checklists, never in a product or test path. The recorded disposition (the private supervise note for fjcloud_dev, `jul26_pm_7 ## Current interventions`) is *resume only on a quiet host*, and `325007bb4` — the merge that carried the predecessor's work — is **not** an ancestor of `main`. Six days have passed with no quiet host, so 'parked on a quiet host' has behaved as 'abandoned without a decision'. **This row is a forcing function, not a plan:** it needs one of three explicit outcomes — resume the lane, salvage the verifier onto `main` under a locality the busy host can satisfy, or record a reasoned ABANDON receipt the way `jul26_pm_6` did for the sibling scale-profile lane. Do not leave it as a fourth state. | Branch: `origin/batman/jul26_am_11_migration_suite_runtime_margin_continuation` (tip `c2ca0c3ea`); predecessor disposition: the private supervise note for fjcloud_dev; abandon-receipt precedent: the reviewed private pm 6 local scale profile tooling disposition receipt. Closing condition — either `git ls-tree -r origin/main --name-only | grep -q verify_migration_import_runtime_margin` succeeds, or a dated ABANDON receipt exists on `main` naming this branch. A branch that merely still exists closes nothing. |
| MIG-17R | Migration-import meta-suite runtime margin is unverified on `main` | **CLOSED 2026-08-03 by the `aug02_11am_7` lane, on exactly the exit this row named.** The single owner `engine/tests/migration_import_contract_test.sh` now asserts a per-run wall-clock ceiling: `MIGRATION_IMPORT_CONTRACT_META_SUITE_RUNTIME_CEILING_SECONDS=2400` owns the threshold and its inline derivation, `assert_meta_suite_runtime_ceiling()` compares terminal full-suite elapsed seconds against it, and `main()` calls it after `assert_terminal_scenario_inventory()` so the ceiling covers the same 162-scenario terminal path rather than a subset or a sibling runner. No second runner, threshold, or registry owner was created. The ceiling was **measured, not chosen**, from two sequential admissible runs with the one-minute load average captured immediately before each: `1205s` at load `84.54` and `683s` at load `13.27`, both `162/162`, exit `0`, cross-checked by `/usr/bin/time -p` (`1206.17s`, `683.93s`). A full-suite negative control proved the guard can fail. Recorded honestly: this is a **loaded-laptop ceiling, not reference-locality evidence** — the host carried five concurrent Flapjack lanes during measurement, and an earlier overlapping attempt was ruled inadmissible rather than averaged in. Receipt: the reviewed private reference scale and runtime margin receipt (bare filename for traceability, unopenable on the mirror: `2026_08_02_aug02_11am_7_reference_scale_and_runtime_margin_receipt.md`). Route this row to the private implementation history at the next history pass. **Original filing follows.** Open, filed 2026-08-02 as the surviving half of `MIG-17`. The ABANDON receipt disposed of the stranded *verifier*; it did not verify the *property*. `main` has no evidence that the migration-import meta-suite runs inside any runtime budget, and no owner asserts one. Filed separately so the closure of `MIG-17` cannot read as coverage. This row is deliberately small: it wants one threshold, derived at a locality this host can actually satisfy under fleet load, asserted by the existing single owner. **Do not re-import the stranded verifier** — its assertions were shown not to describe the current 162-scenario registry, and a second runner/threshold/registry owner is explicitly out of bounds. | Single owner: `engine/tests/migration_import_contract_test.sh` and its `EXPECTED_SCENARIO_IDS` registry (162 scenarios, normalized hash `d892b59c6cd5e8090044e36486ffc79889bbbb8c93fb3eb7c773eadbaabacd57`). Disposition source: the reviewed private migration runtime margin abandon receipt. Falsifiable exit: the contract script asserts a documented per-run wall-clock ceiling, that ceiling was measured on this host with a recorded one-minute load average, and the assertion fails when the ceiling is exceeded. A ceiling chosen without a recorded measurement locality is red. |
| MIG-18 | Bulk-replace rejects oversized uploads only after spooling begins | **CLOSED 2026-08-06 at `52ad0fdc7`, on the exit this row named.** `POST /1/migrations/bulk-replace` now refuses an over-cap upload from its declared `Content-Length` before the body is polled and before the durable spool is touched, returning the handler-owned `413` `{"message":"Bulk replacement payload exceeds the configured limit","status":413}` with zero bytes written. The served HTTP boundary was proven against a running server, not only at the handler: the over-cap request returned `http_code=413` with an empty spool directory, and a `Content-Length`-absent chunked upload remains bounded by the pre-existing streaming cap inside `admit_streamed_payload`, so neither admission path is unguarded. | Owner: `engine/flapjack-http/src/handlers/migration/bulk_replace.rs`; fail-capable contract: `oversized_declared_content_length_is_rejected_before_body_poll` (`bulk_replace.rs:526`). Closing receipt: the reviewed private bulk upload bound and pin scanner rule receipt. |
| MIG-19 | Bulk-replace upload request deadline | **CLOSED 2026-08-02.** The router-wide request timeout used to terminate the measured 1-million-record bulk-replace upload before its body reached durable spool EOF. `enforce_request_timeout` now gives only `POST /1/migrations/bulk-replace` six times the configured request timeout (1,800 seconds at the 300-second default), retaining a finite slow-client bound. Focused tests prove the upload can outlive the global deadline, a stalled bulk upload still times out, and unrelated routes keep the global deadline. | Owner: `engine/flapjack-http/src/router.rs::{enforce_request_timeout,BULK_REPLACE_TIMEOUT_MULTIPLIER}`; proof: `engine/flapjack-http/src/router_inline_tests.rs::{bulk_replace_upload_outlives_global_request_timeout_until_body_eof,bulk_replace_upload_is_still_bounded_against_slow_clients,global_request_timeout_still_bounds_other_routes}`; disposition: `engine/docs2/4_EVIDENCE/2026_08_02_aug02_5am_1_ladder_salvage_and_gate_disposition.md`. |
| PL-15 | Loadtest readiness can accept a foreign listener when ownership arguments are omitted | **CLOSED 2026-08-06 by `aug06_10am_6`, on the exit this row named.** All three live `wait_for_loadtest_health` callers now pass both ownership arguments — `scale_ladder.sh:959`, `scale_ladder_smoke_acceptance.sh:208`, and `pl10_saturation_acceptance.sh:273` each supply the launched server's log path and expected bind address. (`engine/_dev/s/manual-tests/disk_exhaustion_durability_selftest.sh` defines a local `wait_for_loadtest_health() { return 0; }` stub and is not a live caller of the shared helper.) `engine/loadtest/tests/liveness_helpers_acceptance.sh` is the enforcement owner and pins the whole call set by denominator, so a new unowned caller reds the gate rather than passing unnoticed. Falsifiability was proven per caller rather than once: the ownership arguments were removed from **each** of the three in turn, producing three separate captured reds, and each caller's foreign-`200` rejection and launched-server acceptance were both captured. | Owner: `engine/loadtest/lib/loadtest_shell_helpers.sh::wait_for_loadtest_health`; enforcement owner: `engine/loadtest/tests/liveness_helpers_acceptance.sh`. Receipt: the reviewed private loadtest listener-ownership receipt. |
| ING-2 | Next ingestion connector and compatibility catalog | Deferred until a named demand and concrete cost/auth/scale gate exists. PostgreSQL/Supabase, MySQL, object storage, crawler/ecommerce sources, and BigQuery/GA4 remain catalog entries, not shipped connectors; BigQuery is explicitly a named parity gap rather than a free-floating work item. | This row owns the future connector catalog; preserve the shipped CLI boundary in `engine/docs2/FEATURES.md#flapjack-ingest-beta-bounds` rather than expanding ING-1. |
| ADR-0005 OQ4 | Cross-node failover idempotency dedup | v1.1 design resolved. Node-local restart-durable idempotency remains at `${FLAPJACK_DATA_DIR}/_idempotency/cache.db`; cross-node single execution requires a quorum-durable reservation before mutation, a quorum-durable completed result before success acknowledgment, and fail-closed handling of pending or indeterminate claims. | [ADR 0010](engine/docs2/3_IMPLEMENTATION/decisions/active/0010_oq4_cross_node_idempotency_dedup_design.md) |
| PL-11 | Public mirror Laravel Scout CI cleanup | Remove the always-green `integration-laravel-scout` job from the public mirror CI once the Debbie flow reaches the mirror, so CI no longer advertises an unsourced integration stub. This open row is the mirror-CI-cleanup slice only; it is distinct from the closed `PL-11` HA-soak-harness-reliability closeout recorded in the private implementation history, which reused the same ID for a different, already-shipped work item. | Target: `.github/workflows/ci.yml`; owner: mirror/debbie flow; private-development context: the private 20260718 stage5 framework integration portfolio disposition research note |
| PL-2 | `cargo-nextest` migration re-evaluation | Re-evaluate around 2026-11-26 against accumulated hang-frequency data. If PL-1 plus test-hang discipline have not covered 95% of hangs, plan `.config/nextest.toml` per-test timeouts, a `.cargo/config.toml` alias, and CI workflow migration. | Existing CI-side cap owner: `engine/tests/ci_test_timeout_cap_acceptance.sh` |
| DOCS-CLAIMS | Stale docs-site pricing/organization/image claim audit | Audit and correct the public docs-site pricing, organization, and image claims, which live on the separate private `flapjackhq/flapjack-cloud` `docs-site/` surface (not this repo's Debbie sync surface). Entry gate before any edit or deploy: provenance recovery of the editable owner path, plus an automated claim inventory (rendered source → asserted value) that tests correctness, not page existence. No in-repo renderer, second roadmap, or docs-site mirror boundary here. | Research: the private 20260716 stage4 docs site claim followup research note; owner: private `flapjackhq/flapjack-cloud` `docs-site/` |
| MIG-10 | Deferred `max_items_per_resource` operator config override | Deferred and unimplemented. Reuse the existing `SpoolStore::new(data_root, limits)` constructor seam; document any eventual operator key only in `engine/docs2/3_IMPLEMENTATION/OPS_CONFIGURATION.md`, not `engine/docs/CONFIGURATION.md`; keep the default at `1_000_000`, and freeze limits into each manifest so later raises do not retroactively alter in-flight jobs. | Seam: `engine/flapjack-http/src/handlers/migration/spool.rs`; config-doc owner: `engine/docs2/3_IMPLEMENTATION/OPS_CONFIGURATION.md` |
| HA-MEMBERSHIP-UI | Dashboard peer add/remove UI for runtime HA membership | **CLOSED 2026-08-03 by the `aug03_11am_10` lane.** The Cluster screen submits runtime add/remove mutations through the shipped internal endpoints, puts destructive removal behind a named node-scoped confirmation, and covers eight loading/error/workflow states under the cluster screen contract. Served Playwright proof against a replication-enabled server passed 9/9; fixture-layer oracles independently confirmed the added peer present, the removed peer absent, and cleanup at zero peers. Cleartext refusal remains server-owned, and the screen uses the shared HttpOnly session without reading or storing an admin key. | UI owner: `engine/dashboard/src/pages/Cluster.tsx`; API owner: `engine/flapjack-http/src/handlers/internal.rs`; receipt: the reviewed private cluster peer screen receipt. Falsifiable exit: the focused Cluster component contracts and the served `cluster_peers.spec.ts` workflow must pass, with the runtime status oracle proving add, remove, and cleanup values. |
| HA-K8S-OPERATOR | Full Kubernetes operator (CRD/controller lifecycle) | Deferred to a separate decision. Engine-owned dead-peer eviction and returning-peer readmission now exist under `HA-AUTOHEAL`, and the official Helm chart remains the ecosystem-standard external-orchestration story. A CRD/controller-based operator would still need to justify declarative lifecycle orchestration, adoption, and recovery beyond the engine's local quorum guard; the engine binary intentionally does not self-provision, and this row does not settle operator demand. | Helm chart owner: `deploy/helm/flapjack/` (`Chart.yaml`, `templates/statefulset.yaml`, `values.yaml`); render-validation oracle: `deploy/helm/flapjack/test_render.sh`; live chart-adoption proof: `deploy/helm/flapjack/test_live_cluster.sh`; local engine auto-heal boundary: `HA-AUTOHEAL`. |

Detailed working checklists and proof-pack session notes may exist in the private
dev repo, but public routing docs should resolve entirely within the synced
public tree.

# Flapjack — Roadmap

**Last updated:** 2026-05-28
**Status ledger policy:** This file is a routing page only. Canonical product status is maintained in [`engine/docs2/FEATURES.md`](engine/docs2/FEATURES.md).

## Implemented

Implemented status/history is canonical in [`engine/docs2/FEATURES.md#completed-work-archive`](engine/docs2/FEATURES.md#completed-work-archive) and [`engine/docs2/FEATURES.md#public-sync-lineage-ledger-canonical`](engine/docs2/FEATURES.md#public-sync-lineage-ledger-canonical).

Use this file only to route open work; do not duplicate launch/status narration here.
## Open / Not Yet Implemented

Open routing details live here; status/history remains canonical in [`engine/docs2/FEATURES.md`](engine/docs2/FEATURES.md).

| # | Work Item | Status | Routing |
|---|-----------|--------|---------|
| RF-1 | Search HA ownership/freshness design gate | Done (2026-05-24, merge `9240b913`) | Stage 7/7 complete (65/65 checklist); posthoc security review caught + fixed a path-traversal flaw in `engine/_dev/s/lib/sync-core.sh::ensure_safe_destination_path`. Design at `engine/docs2/3_IMPLEMENTATION/decisions/active/0003_search_ha_ownership_freshness.md`; contracts at `engine/tests/ha_contracts/c[1-5]_*.rs`. |
| RF-2 | Durable analytics rollup writer/query planner | Done (2026-05-25, merge `a5413eef`) | Rollup writer ([`engine/src/analytics/writer.rs`](engine/src/analytics/writer.rs)), query-planner fallback ([`engine/src/analytics/query/mod.rs`](engine/src/analytics/query/mod.rs)), and certified-coverage retention gate ([`engine/src/analytics/retention.rs`](engine/src/analytics/retention.rs) line 104-108) all shipped. Rollout note: [`docs/research/may25_pm_1_rf2_rollup_completion_note.md`](docs/research/may25_pm_1_rf2_rollup_completion_note.md). |
| RF-4 | Runbooks iteration | Open | Continue refining `engine/docs2/3_IMPLEMENTATION/OPERATIONS.md` from incident learnings. |
| PL-1 | Test-hang CI-side cap | Done (2026-05-27, merge `4511cb93`) | `timeout-minutes: 10` enforced on the four `cargo nextest run` steps in [`.github/workflows/ci.yml`](.github/workflows/ci.yml) and `nightly.yml`. Acceptance contract at `engine/tests/ci_test_timeout_cap_acceptance.sh`. Discipline rule already landed earlier in [`.scrai/rules.md`](.scrai/rules.md) Rust Conventions block (commit `cf5e3e32`). |
| PL-2 | `cargo-nextest` migration re-eval | Deferred to ~2026-11-26 | Re-evaluate against accumulated hang-frequency data. If discipline (PL-1 + .scrai rules) hasn't covered 95% of hangs by then, plan full migration: `.config/nextest.toml` per-test timeouts, `.cargo/config.toml` alias, CI workflow rewrite. |
| PL-3 | Stale short-query-expansion comment | Done (2026-05-26, `e18e2ca3`) | Doc-only fix landed in `e18e2ca3`: short-query-expansion comment in `engine/src/query/executor/mod.rs` now matches `EdgeNgramTokenFilter` passthrough behavior post-Tantivy 0.26.1. |
| PL-4 | Retire `engine/docs/TANTIVY_FORK_STATUS_NOV19.md` | Done (2026-05-26, `85c77de6`) | Landed in `85c77de6`: stale fork-status doc retired. `EdgeNgramTokenFilter` is canonical at `engine/src/tokenizer/edge_ngram_filter.rs`; tantivy 0.26.1 ships from crates.io. |
| PL-5 | 3-seam `is_private_or_local_ip` dedup | Done (2026-05-26, `59cd1cd9`) | Landed in `59cd1cd9`: duplicated IP-range logic consolidated to canonical helpers in `engine/src/security.rs`, with call sites in `chat.rs`/`vector/config.rs` updated and coverage in `security_tests.rs`. |
| PL-6 | A10 DNS-rebinding TOCTOU residual | Done (2026-05-26, commit f8f2e06b) | Defensive-depth hardening: close the timing window between DNS lookup and outbound HTTP. No known exploit on shipped v1.0.x — defensive only. Anchored to L3 audit residuals; A10 baseline shipped as `19c55d61`. |
| PL-7 | Soak-evidence durability discipline | Done (2026-05-27, merge `a89db0bd`) | "Soak / Integration Script Evidence Durability" rule added to [`.scrai/rules.md`](.scrai/rules.md). Executable acceptance seams: `engine/loadtest/tests/ha_soak_acceptance.sh` + `ha_soak_loadtest_durability_acceptance.sh`. Canonical evidence-preservation pattern anchored to `engine/_dev/s/manual-tests/ha-soak-test.sh` (post-`528235bf`). |
| PL-8 | Nginx restart-window write-loss recovery | Partial (2026-05-27, merge `97bba490`) | Stage 1 + Stage 2 landed; Stage 3 (≤0.3% spread soak validation) deferred. **Shipped:** ADR [`0005_nginx_restart_window_write_recovery.md`](engine/docs2/3_IMPLEMENTATION/decisions/active/0005_nginx_restart_window_write_recovery.md) selected Option C (server-owned idempotency) + Option A (client retry contract); content-hash auto-IDs, `X-Flapjack-Idempotency-Key` TTL cache ([`engine/flapjack-http/src/idempotency.rs`](engine/flapjack-http/src/idempotency.rs)), and `Retry-After: 1` on 429/503 transient errors shipped in commit `d83c6d8e`. Soak-harness reliability fixes (HUP-trap, `finalize_interrupted_soak`, reentrant `cleanup`) shipped alongside, and PL-11 later closed via explicit contract ownership docs + acceptance coverage. **Deferred & why:** 9 Stage-3 soak attempts crashed pre-closeout; the deepest completed segment reached 30 min with 78.43% write-failure rate (worktree-local artifacts; on-main anchor evidence is the 2h soak cited in the PL-10 row below, which shows the same saturation pattern at 82.27%). The dominant residual is write-path saturation, not restart-window loss. Follow-up: PL-10 (saturation). **2026-05-28 update:** Option C itself (ack-on-durable: HTTP 200 contingent on Tantivy commit) tracked under PL-13 — fjcloud A5 soak `20260528T135027Z_NONGREEN/` confirmed `silent_drops=144` is a real restart-window-loss defect (case-1d HTTP 404 disambiguation), separate from saturation. PL-13 ships the implementation Stage 3 was meant to validate. |
| PL-13 | Ack-on-Durable for HTTP batch writes (completes ADR-0005 Option C) | Open | Lane spec: [`chats/icg/may28_pm_1_pl13_ack_on_durable.md`](chats/icg/may28_pm_1_pl13_ack_on_durable.md). HTTP batch-write returns 200 only after Tantivy commit durability; queue-full / commit-failure / ack-timeout return 5xx with `Retry-After`. Allowed-zone surface per ADR-0005 "Downstream Contract Notes". Acceptance: fjcloud A5 re-soak emits GREEN bundle with `silent_drops=0` and `visible_in_search_after=writes_attempted` (≤1% tolerance for replication jitter). **Blocks v1.0.2 promote:** Lane D [`chats/icg/may27_8pm_4_release_cut_v1_0_2.md`](chats/icg/may27_8pm_4_release_cut_v1_0_2.md) Stage 4 is on SUPERVISOR-HOLD until this lane lands and A5 re-soaks GREEN (auto-unhold pre-authorized 2026-05-28). |
| PL-9 | Rollup window granularity test-overridable for soak proofs | Done (2026-05-27, commits `9353fca8` + `05b4ae8b`) | `FLAPJACK_ROLLUP_WINDOW_OVERRIDE_MS` now resolves through [`engine/src/analytics/mod.rs`](engine/src/analytics/mod.rs), is consumed by the HTTP scheduler in [`engine/flapjack-http/src/background_tasks.rs`](engine/flapjack-http/src/background_tasks.rs) and rollup validation in [`engine/src/analytics/writer.rs`](engine/src/analytics/writer.rs), and is passed through the soak harness in [`engine/loadtest/soak_proof.sh`](engine/loadtest/soak_proof.sh). Executable acceptance owners: [`engine/loadtest/tests/soak_proof_analytics_acceptance.sh`](engine/loadtest/tests/soak_proof_analytics_acceptance.sh) and [`engine/loadtest/tests/analytics_soak_window_override_acceptance.sh`](engine/loadtest/tests/analytics_soak_window_override_acceptance.sh). |
| PL-10 | Write-path saturation under sustained load | Open | Stage 6 dual-scenario revalidation completed on 2026-05-28 and preserved contract health while confirming ongoing saturation behavior. Mixed-soak (60m) and realistic-batch (30m) both passed contract-health gates (`write_http_5xx_rate=0.00%`, `write_http_unexpected_4xx_rate=0.00%`) from saved stdout classification, but write-failure remained high under expected overload (`85.04%` and `98.21%`, respectively). Canonical measured artifacts: [`engine/docs/research/pl10_stage6_dual_scenario_classification.md`](engine/docs/research/pl10_stage6_dual_scenario_classification.md), [`engine/loadtest/BENCHMARKS.md`](engine/loadtest/BENCHMARKS.md), and retained bundle [`engine/loadtest/results/20260528T062547Z-pl10-stage6-dual-scenario/`](engine/loadtest/results/20260528T062547Z-pl10-stage6-dual-scenario/). PL-10 remains open for additional write-path saturation remediation and revalidation. |
| PL-11 | HA soak harness reliability | Done (2026-05-27, commits `5dd74bb3`, `004118c0`) | Interrupt-closeout contract is now documented and enforced at its canonical owners: `engine/loadtest/tests/ha_soak_acceptance.sh` and the harness header comment in `engine/_dev/s/manual-tests/ha-soak-test.sh`. |
| PL-12 | HA peer-failed / replication amplification follow-up | Partial (2026-05-28, merge `c0938224`) | Stage 1 diagnostic-test contract shipped: working probe at [`engine/_dev/s/manual-tests/ha-peer-failed-amp-probe.sh`](engine/_dev/s/manual-tests/ha-peer-failed-amp-probe.sh), working acceptance contract at [`engine/loadtest/tests/ha_peer_failed_amplification_acceptance.sh`](engine/loadtest/tests/ha_peer_failed_amplification_acceptance.sh) (both modes pass), 5-run baseline at [`docs/research/pl12_stage1_baseline.md`](docs/research/pl12_stage1_baseline.md), evidence notes at [`docs/research/pl12_stage1_evidence_notes.md`](docs/research/pl12_stage1_evidence_notes.md). **Diagnosed root cause:** ratio metric `(peer_down+1)/(baseline+1)` degenerates when steady-state baseline ≈ 0 — supervisor note at Stage 1 checklist prescribes absolute-count contract replacement. **Open follow-up (file PL-12 v2):** (1) replace ratio gate with absolute `raw_peer_down_count` threshold contract, (2) stop overriding `FLAPJACK_LOADTEST_READ_INDEX` to write index (needs read-index seed in HA probe mode), (3) ship the circuit-breaker tune Stage 2 was scoped to do. Lane B closed at Stage 1/4 after 13h matt review-loop pathology — agent shipped real value (probe + acceptance + 5-run baseline + diagnosis) but couldn't converge Stage 1 closeout autonomously. |
## Open-Source Launch Planning

For launch sequencing and current status, use:

- [`engine/docs2/FEATURES.md#recommended-execution-order`](engine/docs2/FEATURES.md#recommended-execution-order)
- [`engine/docs2/FEATURES.md#current-production-readiness-state`](engine/docs2/FEATURES.md#current-production-readiness-state)
- [`engine/docs2/1_STRATEGY/HIGHEST_PRIORITY.md`](engine/docs2/1_STRATEGY/HIGHEST_PRIORITY.md)

## Confidence-Completeness Planning

For the next engineering block after the closed launch gate, use:

- [`engine/docs2/FEATURES.md#recommended-execution-order`](engine/docs2/FEATURES.md#recommended-execution-order)
- [`engine/docs2/1_STRATEGY/HIGHEST_PRIORITY.md`](engine/docs2/1_STRATEGY/HIGHEST_PRIORITY.md)
- [`PRIORITIES.md`](PRIORITIES.md)

Detailed working checklists and proof-pack session notes may exist in the private dev repo, but public routing docs should resolve entirely within the synced public tree.

## Archive

When this list grows stale or too large, move completed items to `roadmap-history/YYYY-QN.md`.

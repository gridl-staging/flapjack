# Flapjack — Roadmap

**Last updated:** 2026-06-02
**Status ledger policy:** This file is a routing page only. Canonical product status is maintained in [`engine/docs2/FEATURES.md`](engine/docs2/FEATURES.md).

## Implemented

Implemented status/history is canonical in [`engine/docs2/FEATURES.md#completed-work-archive`](engine/docs2/FEATURES.md#completed-work-archive) and [`engine/docs2/FEATURES.md#public-sync-lineage-ledger-canonical`](engine/docs2/FEATURES.md#public-sync-lineage-ledger-canonical).

Use this file only to route open work; do not duplicate launch/status narration here.
## Open / Not Yet Implemented

Open routing details live here; status/history remains canonical in [`engine/docs2/FEATURES.md`](engine/docs2/FEATURES.md).

**ID prefixes:** `RF-*` = foundational refinement track (the Confidence-Completeness Planning work: HA contracts, durable analytics, runbooks); `PL-*` = launch-hardening / operational-polish track (CI caps, soak-evidence durability, write-path durability & saturation). IDs are stable identifiers, not priority rank — see [`PRIORITIES.md`](PRIORITIES.md) for ordering.

| # | Work Item | Status | Routing |
|---|-----------|--------|---------|
| RF-1 | Search HA ownership/freshness design gate | Done (2026-05-24, merge `9240b913`) | Stage 7/7 complete (65/65 checklist); posthoc security review caught + fixed a path-traversal flaw in the private sync helper. Design detail is retained in private ADR evidence; contracts live at `engine/tests/ha_contracts/c[1-5]_*.rs`. |
| RF-2 | Durable analytics rollup writer/query planner | Done (2026-05-25, merge `a5413eef`) | Rollup writer ([`engine/src/analytics/writer.rs`](engine/src/analytics/writer.rs)), query-planner fallback ([`engine/src/analytics/query/mod.rs`](engine/src/analytics/query/mod.rs)), and certified-coverage retention gate ([`engine/src/analytics/retention.rs`](engine/src/analytics/retention.rs) line 104-108) all shipped. Rollout note retained in private stage evidence. |
| RF-4 | Runbooks iteration | Open | Continue refining `engine/docs2/3_IMPLEMENTATION/OPERATIONS.md` from incident learnings. |
| PL-1 | Test-hang CI-side cap | Done (2026-05-27, merge `4511cb93`) | `timeout-minutes: 10` enforced on the four `cargo nextest run` steps in [`.github/workflows/ci.yml`](.github/workflows/ci.yml) and `nightly.yml`. Acceptance contract at `engine/tests/ci_test_timeout_cap_acceptance.sh`. Discipline rule already landed earlier in private operator rules (commit `cf5e3e32`). |
| PL-2 | `cargo-nextest` migration re-eval | Deferred to ~2026-11-26 | Re-evaluate against accumulated hang-frequency data. If discipline (PL-1 + .scrai rules) hasn't covered 95% of hangs by then, plan full migration: `.config/nextest.toml` per-test timeouts, `.cargo/config.toml` alias, CI workflow rewrite. |
| PL-3 | Stale short-query-expansion comment | Done (2026-05-26, `e18e2ca3`) | Doc-only fix landed in `e18e2ca3`: short-query-expansion comment in `engine/src/query/executor/mod.rs` now matches `EdgeNgramTokenFilter` passthrough behavior post-Tantivy 0.26.1. |
| PL-4 | Retire `engine/docs/TANTIVY_FORK_STATUS_NOV19.md` | Done (2026-05-26, `85c77de6`) | Landed in `85c77de6`: stale fork-status doc retired. `EdgeNgramTokenFilter` is canonical at `engine/src/tokenizer/edge_ngram_filter.rs`; tantivy 0.26.1 ships from crates.io. |
| PL-5 | 3-seam `is_private_or_local_ip` dedup | Done (2026-05-26, `59cd1cd9`) | Landed in `59cd1cd9`: duplicated IP-range logic consolidated to canonical helpers in `engine/src/security.rs`, with call sites in `chat.rs`/`vector/config.rs` updated and coverage in `security_tests.rs`. |
| PL-6 | A10 DNS-rebinding TOCTOU residual | Done (2026-05-26, commit f8f2e06b) | Defensive-depth hardening: close the timing window between DNS lookup and outbound HTTP. No known exploit on shipped v1.0.x — defensive only. Anchored to L3 audit residuals; A10 baseline shipped as `19c55d61`. |
| PL-7 | Soak-evidence durability discipline | Done (2026-05-27, merge `a89db0bd`) | "Soak / Integration Script Evidence Durability" rule added to private operator rules. Executable acceptance seams: `engine/loadtest/tests/ha_soak_acceptance.sh` + `ha_soak_loadtest_durability_acceptance.sh`. Canonical evidence-preservation pattern is retained in private soak-harness evidence (post-`528235bf`). |
| PL-8 | Nginx restart-window write-loss recovery | **Core closed 2026-05-29** (idempotency cache `97bba490` + PL-13 ack-on-durable + A5 GREEN); residuals → PL-10/PL-14/OQ2 | Stage 1 + Stage 2 landed; Stage 3 (≤0.3% spread soak validation) deferred. **Shipped:** ADR `0005_nginx_restart_window_write_recovery.md` selected Option C (server-owned idempotency) + Option A (client retry contract); content-hash auto-IDs, `X-Flapjack-Idempotency-Key` TTL cache ([`engine/flapjack-http/src/idempotency.rs`](engine/flapjack-http/src/idempotency.rs)), and `Retry-After: 1` on 429/503 transient errors shipped in commit `d83c6d8e`. Soak-harness reliability fixes (HUP-trap, `finalize_interrupted_soak`, reentrant `cleanup`) shipped alongside, and PL-11 later closed via explicit contract ownership docs + acceptance coverage. **Deferred & why:** 9 Stage-3 soak attempts crashed pre-closeout; the deepest completed segment reached 30 min with 78.43% write-failure rate (worktree-local artifacts; on-main anchor evidence is the 2h soak cited in the PL-10 row below, which shows the same saturation pattern at 82.27%). The dominant residual is write-path saturation, not restart-window loss. Follow-up: PL-10 (saturation). **2026-05-29 update — restart-window write-loss CLOSED + validated.** PL-13 (`6af7ad92`) shipped the ack-on-durable implementation Stage 3 was meant to validate; validated by fjcloud's A5 re-soak on the PL-13 engine. **Canonical, live validation status is cross-repo in the fjcloud launch-verification matrix (do not restate it here).** The `20260528T135027Z_NONGREEN/` soak had confirmed `silent_drops=144` was a real restart-window-loss defect (case-1d HTTP 404), separate from saturation; that defect is now validated-fixed. Residuals remain SEPARATE open items: write-path saturation (PL-10); cross-node idempotency-cache durability (ADR-0005 OQ2, deferred); delete-path bounding + eviction race (PL-14). **2026-05-31 v1.0.4 followthrough — ADR-0005 OQ1/OQ2/OQ3 closed via Lane D (merge `99d2e5b2`):** node-local idempotency durability is now persisted at `${FLAPJACK_DATA_DIR}/_idempotency/cache.db` (SQLite). Restart replay preserves single-execution semantics for repeated idempotency keys. Owner: [`engine/flapjack-http/src/idempotency.rs`](engine/flapjack-http/src/idempotency.rs). ADR-0005 OQ4 (cross-node failover-resilient dedup) remains explicitly DEFERRED to v1.1 — a peer restart during the same idempotency-key window can still cause re-execution because the cache is node-local. |
| PL-13 | Ack-on-Durable for HTTP batch writes (completes ADR-0005 Option C) | **Done (2026-05-29) — implemented `6af7ad92`, A5-validated** | ADR-0005 Option C implemented: HTTP batch-write 200 contingent on Tantivy commit durability; queue-full → 429, commit-failure/ack-timeout → 503, all with `Retry-After: 1`. Allowed-zone surface per ADR-0005 "Downstream Contract Notes": `manager/write.rs`, `objects/batch.rs`, `objects/mod.rs`, `error.rs`. 120.7x single-doc throughput reduction accepted (durability > throughput). **Acceptance:** validated by fjcloud's A5 HA re-soak on the PL-13 engine (baked into the staging AMI via the fjcloud PL-13 staging lane). **Canonical, live validation status lives in the fjcloud launch-verification matrix — do not restate it here (it goes stale across repos; the prior inline "§5 live / GREEN bundle" restatement was stale within a day).** Follow-up gaps (delete-path bounding + eviction race) tracked under PL-14. Soak handoff and lane spec are retained in private stage evidence. |
| PL-14 | Durable-write completeness follow-ups (PL-13 gaps found 2026-05-29 code review) | **Done (2026-05-29, merge `dbd78016`)** | Both gaps closed. **(1) Deletes bounded.** `delete_documents_durable` introduced at [`engine/src/index/manager/write.rs:368`](engine/src/index/manager/write.rs#L368) (mirrors `add_documents_durable` ack-on-durable semantics → 503 with `Retry-After: 1` on accepting-node restart instead of unbounded hang). Delete callers in [`engine/flapjack-http/src/handlers/objects/batch.rs:341,359`](engine/flapjack-http/src/handlers/objects/batch.rs#L341), [`engine/flapjack-http/src/handlers/objects/mod.rs`](engine/flapjack-http/src/handlers/objects/mod.rs), and [`engine/flapjack-http/src/handlers/replicas.rs`](engine/flapjack-http/src/handlers/replicas.rs) routed through the new `delete_documents_durable_task_aware` shim. **(2) Eviction skips non-terminal tasks.** `evict_old_tasks` in [`engine/src/index/manager/mod.rs`](engine/src/index/manager/mod.rs) now skips `Enqueued`/`Processing` tasks so no in-flight write becomes a spurious `TaskNotFound`. Lane spec and Stage 3 accepted-partial evidence are retained in private stage evidence; the engine fix is test-covered by Stages 1-2 and did not gate v1.0.3. |
| PL-9 | Rollup window granularity test-overridable for soak proofs | Done (2026-05-27, commits `9353fca8` + `05b4ae8b`) | `FLAPJACK_ROLLUP_WINDOW_OVERRIDE_MS` now resolves through [`engine/src/analytics/mod.rs`](engine/src/analytics/mod.rs), is consumed by the HTTP scheduler in [`engine/flapjack-http/src/background_tasks.rs`](engine/flapjack-http/src/background_tasks.rs) and rollup validation in [`engine/src/analytics/writer.rs`](engine/src/analytics/writer.rs), and is passed through the soak harness in `engine/loadtest/soak_proof.sh`. Executable acceptance owners: `engine/loadtest/tests/soak_proof_analytics_acceptance.sh` and `engine/loadtest/tests/analytics_soak_window_override_acceptance.sh`. |
| PL-10 | Write-path saturation under sustained load | Mitigation shipped v1.0.4 (`FLAPJACK_WRITE_QUEUE_BATCH_SIZE` tunable, merge `c7fcbc9e`); **v1.0.5 Lane C redesigned the acceptance harness gate to `uplift_ratio >= 1.50`, yielding deterministic `TUNABLE_VERIFIED`**; cross-node fanout remains v1.1 | Stage 6 dual-scenario revalidation completed on 2026-05-28 and preserved contract health while confirming ongoing saturation behavior. Mixed-soak (60m) and realistic-batch (30m) both passed contract-health gates (`write_http_5xx_rate=0.00%`, `write_http_unexpected_4xx_rate=0.00%`) from saved stdout classification, but write-failure remained high under expected overload (`85.04%` and `98.21%`, respectively). Stage 6 canonical results directory: `engine/loadtest/results/20260528T062547Z-pl10-stage6-dual-scenario/`. Canonical measured artifacts: `engine/docs/research/pl10_stage6_dual_scenario_classification.md` and [`engine/loadtest/BENCHMARKS.md`](engine/loadtest/BENCHMARKS.md). **v1.0.4 followthrough:** Lane F (merge `c7fcbc9e`) shipped the `FLAPJACK_WRITE_QUEUE_BATCH_SIZE` env var (default commit threshold `32`); acceptance owner remains `engine/loadtest/tests/pl10_saturation_acceptance.sh`, with comparator ownership at `engine/loadtest/tests/pl10_saturation_acceptance.sh::evaluate_and_persist_stage3_comparison()`. The exact three-run proof set that satisfied Stage 3's exit criterion is `engine/loadtest/results/20260601T202043Z-pl10-saturation-acceptance/`, `engine/loadtest/results/20260601T203717Z-pl10-saturation-acceptance/`, and `engine/loadtest/results/20260601T204623Z-pl10-saturation-acceptance/`: baseline_unset `successful_writes` = `29128`, `13448`, `23304`; tuned_batch64 `successful_writes` = `54728`, `46917`, `45512`; comparison verdict `TUNABLE_VERIFIED` with uplift ratios `1.8789`, `3.4888`, `1.9530`; and per-case `failure_pct=0.00` / `write_http_5xx_rate_pct=0.00` / `write_http_unexpected_4xx_rate_pct=0.00` / `k6_exit_code=0`. Lane spec for the v1.0.5 harness-gate redesign that yielded the `uplift_ratio >= 1.50` deterministic gate: `chats/icg/jun01_am_3_pl10_harness_gate_redesign.md`. Cross-node fanout remains v1.1 (single-writer Tantivy mutex `Arc<Mutex<ManagedIndexWriter>>` is the architectural ceiling). PL-10 remains open for further saturation remediation. |
| PL-11 | HA soak harness reliability | Done (2026-05-27, commits `5dd74bb3`, `004118c0`) | Interrupt-closeout contract is now documented and enforced by `engine/loadtest/tests/ha_soak_acceptance.sh`, with private harness commentary retained in stage evidence. |
| PL-12 | HA peer-failed / replication amplification follow-up | Done (2026-05-29) | Absolute-count contract shipped: acceptance script `engine/loadtest/tests/ha_peer_failed_amplification_acceptance.sh` gates on `MIN_PEER_DOWN <= raw_peer_down_count <= MAX_PEER_DOWN` (ratio gate retired), with private probe evidence covering the upper bound + read-index override fix. Calibrated bound `MAX_PEER_DOWN_LITERAL=94` via the high-variance fallback (`CV > 0.30`). Stage-3 circuit-breaker tune resolved as no-tune (keep `DEFAULT_FAILURE_THRESHOLD=3`) on like-for-like cap-hit evidence; calibration and tune evidence are retained in private stage artifacts. |
## Open-Source Launch Planning

For launch sequencing and current status, use:

- [`engine/docs2/FEATURES.md#recommended-execution-order`](engine/docs2/FEATURES.md#recommended-execution-order)
- [`engine/docs2/FEATURES.md#current-production-readiness-state`](engine/docs2/FEATURES.md#current-production-readiness-state)
- [`engine/docs2/1_STRATEGY/HIGHEST_PRIORITY.md`](engine/docs2/1_STRATEGY/HIGHEST_PRIORITY.md)

## Confidence-Completeness Planning

For the next engineering block after the closed launch gate, use:

- [`engine/docs2/FEATURES.md#recommended-execution-order`](engine/docs2/FEATURES.md#recommended-execution-order)
- [`engine/docs2/1_STRATEGY/HIGHEST_PRIORITY.md`](engine/docs2/1_STRATEGY/HIGHEST_PRIORITY.md)
- [`PRIORITIES.md`](PRIORITIES.md)

Detailed working checklists and proof-pack session notes may exist in the private dev repo, but public routing docs should resolve entirely within the synced public tree.

## Archive

When this list grows stale or too large, move completed items to `roadmap-history/YYYY-QN.md`.

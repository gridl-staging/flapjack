# Flapjack — Priorities

**Last updated:** 2026-05-28

Canonical priority details are maintained in [`engine/docs2/1_STRATEGY/HIGHEST_PRIORITY.md`](engine/docs2/1_STRATEGY/HIGHEST_PRIORITY.md).

## Summary

All launch-credible contracts closed. v1.0.x released across full SDK matrix. The may24_816am batch closed the last Lane F gaps: engine repo transferred to `flapjackhq/flapjack` (preserves history + GHCR + releases + auto-redirect), npm `flapjack-search@1.0.0` + 6 `@flapjack-search/*` scoped packages published via OIDC Trusted Publishing on `flapjackhq/flapjack-search-javascript`, GHCR multi-arch (`linux/amd64` + `linux/arm64`) backfilled to `ghcr.io/flapjackhq/flapjack:{1.0.0,1.0.1,latest}` via server-side retag. Algolia drop-in story now ships: `npm install flapjack-search` resolves to `1.0.0` with all workspace deps at `^1.0.0`. Focus pivots to RF-2 (analytics rollup writer/planner; Wave 2 dispatched) and RF-1 (Search HA contracts; Wave 3 dispatched). Canonical detail in [`engine/docs2/FEATURES.md`](engine/docs2/FEATURES.md).

### Completed (most recent first)

1. ~~**PL-9 — Rollup window granularity test-overridable**~~ — ✅ Resolved (2026-05-27, commits `9353fca8` + `05b4ae8b`). `FLAPJACK_ROLLUP_WINDOW_OVERRIDE_MS` now resolves through `engine/src/analytics/mod.rs`, is consumed by `engine/flapjack-http/src/background_tasks.rs` + `engine/src/analytics/writer.rs`, and is passed through `engine/loadtest/soak_proof.sh`. Executable proofs: `engine/loadtest/tests/soak_proof_analytics_acceptance.sh` and `engine/loadtest/tests/analytics_soak_window_override_acceptance.sh`.

2. ~~**OpenAPI snapshot follow-through**~~ — ✅ Resolved (2026-05-23). Stage 1 confirmed the focused OpenAPI drift assertion is green at current `main`, and Stage 2 confirmed regeneration was a no-op; canonical lineage stays in [`engine/docs2/FEATURES.md#public-sync-lineage-ledger-canonical`](engine/docs2/FEATURES.md#public-sync-lineage-ledger-canonical).

3. ~~**OSS polish wave: lanes A–F**~~ — ✅ All 6 branches merged (2026-05-22 → 2026-05-23). Lane A: `griddlehq/flapjack` cutover + Cloudflare redirect + legacy archive done. Lane B: `engine/rust-toolchain.toml` pinned to 1.95.0, nightly workflow JS/Go/server-backed jobs aligned with CI parity. Lane C: root + engine READMEs polished, CONTRIBUTING/SECURITY/CHANGELOG audited, SDK READMEs cleaned, root clutter sweep + consolidated outbound-link audit done. Lane D: docs site + flapjack-demo + apex `flapjack-website` redeployed via Cloudflare Pages, ≥10-URL sitemap-first audit clean. Lane E: SDK release lane finished without registry publish; Stage 1 audit explicitly mapped JS/Python/Go canonical owners to `flapjackhq/*` and recorded `proceed-with-doc-only` / `stop` / `stop` rather than publish from this mirror. Lane F: 8 stages of e2e evidence captured; final verdict `FAIL` with three named gaps still open. Followup facet-rebuild fixes landed on `main` ([`ed6cc162`](engine/flapjack-http/src/handlers/settings.rs), [`5abbba8a`](engine/flapjack-http/src/handlers/settings.rs)) after Stage 5 evidence was written; Lane F has not been re-scored against these fixes.
4. ~~**Test hygiene, SDK contract CI, and query safety audit**~~ — ✅ Resolved with deferrals (2026-04-15). Public CI now runs the engine SDK contract tests, dashboard/browser false-positive patterns were tightened, stale `_dev/s` test shims were removed in favor of `engine/s/test`, targeted test coverage was added, and confirmed filter/experiment error paths now return typed/sanitized errors. OpenAPI snapshot follow-up remained deferred in the session handoff.
5. ~~**Analytics retention hardening + rollup foundation**~~ — ✅ Retention resolved; rollup foundation merged (2026-04-15). Partition retention is deterministic and still defaults to 90 days. Durable rollup design, known-answer query contracts, schema/config helpers, and `RollupManifest` foundation are merged. Rollup writer/query-planner/gated deletion remain open.
6. ~~**Targeted cleanup follow-through**~~ — ✅ Resolved (2026-04-08). Dashboard experiment normalization/results typing moved into `engine/dashboard/src/lib/experiment-normalization.ts`, and a stale server cognitive-complexity suppression was removed.
7. ~~**Experiment handler merge guardrails**~~ — ✅ Resolved (2026-03-31). `/2/abtests/{id}/results` now routes through the shared store-and-ID resolver seam, direct results-endpoint proofs cover store-unavailable plus numeric/UUID resolution, and experiment OpenAPI docs now declare resolver-driven `500` paths consistently. Published in the completed public lineage (see the canonical ledger in `engine/docs2/FEATURES.md`).
8. ~~**Runbook parity + admin-key truth sync**~~ — ✅ Resolved (2026-03-31). Standardized `flapjack --data-dir <path> reset-admin-key` across startup output, dashboard auth help, internal auth docs, and operator runbooks, including shell-safe quoting for spaced data-dir paths. Published in the completed public lineage (see the canonical ledger in `engine/docs2/FEATURES.md`).
9. ~~**Security baseline follow-through**~~ — ✅ Resolved (2026-03-31). Closed the scoped HTTP-hardening proof gaps by adding invalid-key non-consumption and `FLAPJACK_MAX_BODY_MB` body-limit `413` tests, aligning the security docs/audit, refreshing the committed OpenAPI export, and tightening helper-script safety around sync destinations. Published in the completed public lineage (see the canonical ledger in `engine/docs2/FEATURES.md`).
10. ~~**Security baseline docs + test coverage**~~ — ✅ Resolved (2026-03-31). `SECURITY_BASELINE.md` and the audit matrix now capture the shipped CORS/body-limit/trusted-proxy/rate-limit surface with focused proof references, and `startup_catchup.rs` now treats write-queue timeout as warn-and-continue outside strict bootstrap. Published in the completed public lineage (see the canonical ledger in `engine/docs2/FEATURES.md`).
11. ~~**Nightly CI + sync hygiene**~~ — ✅ Resolved (2026-03-31). Restored nightly Rust CI parity by creating the dashboard dist stub, added `CHANGELOG.md` / `CONTRIBUTING.md` / `SECURITY.md` to the public sync whitelist, and clarified README vector/hybrid support caveats by binary target. Published in the completed public lineage (see the canonical ledger in `engine/docs2/FEATURES.md`).
12. ~~**Operations runbook hardening**~~ — ✅ Resolved (2026-03-31). `OPERATIONS.md` now carries proof-backed startup/readiness/replication/admin-key/snapshot failure runbooks, stronger ownership links to deployment/security/config docs, verified proof citations, and the corrected `flapjack --data-dir <path> reset-admin-key` syntax. Published in the completed public lineage (see the canonical ledger in `engine/docs2/FEATURES.md`).
13. ~~**Debbie sync wave 3**~~ — ✅ Resolved (2026-03-31). Published the last wave of post-launch hardening to staging (`6166055`, CI run `23818440499`) and prod (`b7841a0`, CI run `23819698304`): public-doc sync contract, HA boundary truth surfaces, regression-gate follow-through, and the refreshed committed OpenAPI export.
14. ~~**HA convergence contract + runbook truth sync**~~ — ✅ Resolved (2026-03-31). Boundary path selected. Added `engine/docs2/4_EVIDENCE/HA_CONVERGENCE_ANALYSIS.md`, aligned `OPERATIONS.md` and the HA example README to the proven async-replication boundary, and tightened `engine/loadtest/tests/ha_soak_acceptance.sh`.
15. ~~**Tour walkthrough closure + tour typecheck unblock**~~ — ✅ Resolved (2026-03-30). Closed the former vector/chat blockers (specs 05/06), archived MP4s for all 24/24 per-feature tours, default-enabled vector/local embedding support in the build, wired local AI-provider env bridging, and fixed `npx tsc --noEmit -p tsconfig.tour.json` by adding local Node types plus aligning tour typings with the real `VideoTour` API.
16. ~~**HA multi-node soak execution + truth sync**~~ — ✅ Executed (2026-03-30). The 2h 3-node soak proof pack is now retained at `engine/loadtest/results/20260330T211227Z-ha-soak/`. Final classification was `warning-findings` with `diverged` convergence after 39 restart rotations in the nginx-routed example topology. See [`engine/loadtest/BENCHMARKS.md`](engine/loadtest/BENCHMARKS.md).
17. ~~**Cognitive complexity reduction**~~ — ✅ Resolved (2026-03-30). Decomposed 5 high-CC hotspots: `merge_settings_payload` (CC=35), `validate` (CC=29), `compute_exact_vs_prefix_bucket` (CC=26), `build_results_response` (CC=22), `browse_index` (CC=21). Added settings characterization tests.
18. ~~**Debbie sync pipeline wave 2**~~ — ✅ Resolved (2026-03-30). Full sync to staging+prod. OpenAPI test dedup, experiment handler extraction, soak proof improvements.
19. ~~**Full regression gate + targeted fixes**~~ — ✅ Resolved (2026-03-31). Ran the full post-merge regression gate, fixed FastEmbed local-embedder test nondeterminism by serializing the affected ONNX/model-cache tests, captured proof artifacts in `engine/state/`, and re-synced the committed OpenAPI export after restoring real browse/experiment endpoint summaries.
20. ~~**Public doc sync surface hardening**~~ — ✅ Resolved (2026-03-31). `.debbie.toml` now explicitly syncs the canonical public doc graph, `engine/tests/validate_sync_surface.sh` and shared doc-sync helpers are in place, `validate_doc_links.sh` covers the widened public graph, and public docs no longer refer to dev-only `_dev/s/` multi-instance helper scripts.
21. ~~**File size violations**~~ — ✅ Resolved (2026-03-29). All 20 files brought under 800-line guardrail via 13 test module extractions + 2 production code splits. 0 violations remaining.
22. ~~**GitHub release v1.0.0**~~ — ✅ Done (2026-03-29). 5 binary targets + Docker image published. Release run `23721789375`, CI run `23721442173`.
23. ~~**Debbie sync to staging**~~ — ✅ Resolved (2026-03-29). Pushed v1.0.0 + OTEL + TODO cleanup + codebase quality fixes + OpenAPI regen. CI green.
24. ~~**Post-merge regression validation**~~ — ✅ Resolved (2026-03-29). Full test suite green: 1546 (flapjack) + 1429 (flapjack-http) + server/ssl/replication crates.
25. ~~**OpenAPI spec CI fix**~~ — ✅ Resolved (2026-03-29). Root cause: 18 TODO stubs on utoipa-annotated handlers caused spec mismatch after scrai strip. Replaced with real doc comments.
26. ~~**Codebase quality cleanup**~~ — ✅ Resolved (2026-03-29). Fixed 15 error-leaking 500 sites, migrated 5 handler files to `HandlerError`, decomposed `execute_search_query`, removed cognitive_complexity suppressions.
27. ~~**TODO stub cleanup**~~ — ✅ Resolved (2026-03-29). Replaced ~601 auto-generated `TODO: Document` stubs with real doc comments. HA soak test script added.
28. ~~**Distributed tracing**~~ — ✅ Resolved (2026-03-28). OTLP gRPC export shipped behind `otel` feature flag.
29. ~~**OSS packaging**~~ — ✅ Resolved (2026-03-28). SECURITY.md, CHANGELOG.md, CONTRIBUTING.md shipped. All crates at 1.0.0.
30. ~~**Overload interpretation / tuning**~~ — ✅ Resolved (2026-03-28). Soak scenarios use `SOAK_WRITE_THRESHOLDS`.

### Completed (continued)

31. ~~**Wave 2 — RF-2 analytics rollup writer/query planner**~~ — ✅ Resolved (2026-05-25, merge `a5413eef`). Rollup writer + query-planner fallback + certified-coverage retention gate all shipped. Canonical in [`ROADMAP.md`](ROADMAP.md) RF-2 row and [`engine/docs2/FEATURES.md`](engine/docs2/FEATURES.md) "Durable analytics rollup storage".
32. ~~**Wave 3 — RF-1 Search HA contracts**~~ — ✅ Resolved (2026-05-24, merge `9240b913`). Stage 7/7 complete; posthoc security review caught + fixed a path-traversal flaw in `engine/_dev/s/lib/sync-core.sh`. Canonical in [`ROADMAP.md`](ROADMAP.md) RF-1 row.
33. ~~**May 26-27 polish wave**~~ — ✅ Resolved (2026-05-27). Wave 1 doc-unification (b356e), Wave 2 Lane A PL-3/PL-4/PL-5 cleanup (3d1c1), Lane B PL-1 CI cap (21930), Lane C PL-6 DNS-rebinding (7fa56), Lane D PL-7 soak-evidence (1d892) all merged. Wave 3 PL-8 closed as Partial (d4d82 → merge 97bba490): Stage 1+2 idempotency landed; PL-11 closed via the shipped interrupt-closeout contract docs + acceptance coverage (`5dd74bb3`, `004118c0`); Stage 3 soak validation remains deferred pending PL-10 (write-path saturation).

### Active

1. **PL-13 — Ack-on-Durable for HTTP batch writes** — Open, vetted matt lane (~1–2 weeks wall). Completes ADR-0005 Option C: HTTP batch-write 200 only after Tantivy commit durability. fjcloud A5 soak `20260528T135027Z_NONGREEN/` proved current code violates the contract — 144 writes returned 200 then disappeared post-restart, `doc-1100008` requeries HTTP 404. **Blocks v1.0.2 promote:** Lane D [`chats/icg/may27_8pm_4_release_cut_v1_0_2.md`](chats/icg/may27_8pm_4_release_cut_v1_0_2.md) Stage 4 is on SUPERVISOR-HOLD until PL-13 lands and A5 re-soaks GREEN (auto-unhold pre-authorized 2026-05-28). Lane spec: [`chats/icg/may28_pm_1_pl13_ack_on_durable.md`](chats/icg/may28_pm_1_pl13_ack_on_durable.md). Durability ranks above throughput (PL-10) for this window.
2. **Lane B Stage 4–5 — Post-cutover nightly verification** — Wall-time gate; needs 3 consecutive green nightlies on `flapjackhq/flapjack`. 2026-05-28 triage: nightly red since transfer was stale-publish on Rust+Installer (dev `main` has the fixes already) plus a pre-existing Laravel Scout stub that the org transfer stopped masking. Dropped `integration-laravel-all` from `nightly.yml` until the integration is real; `debbie sync staging` → `debbie sync prod` publishes the Rust/Installer fixes.
3. **Runbooks iteration** — Operational routing stays in [`engine/docs2/3_IMPLEMENTATION/OPERATIONS.md`](engine/docs2/3_IMPLEMENTATION/OPERATIONS.md).
4. **PL-10 — Write-path saturation under sustained load** — Open, ~8-16h matt lane. Stage 6 dual-scenario revalidation (2026-05-28) confirmed public write contract health remains intact across both canonical soaks (zero write `5xx`, zero unexpected write `4xx`) while sustained overload still yields high expected write saturation (`85.04%` mixed 60m, `98.21%` realistic-batch 30m). Canonical evidence: `engine/docs/research/pl10_stage6_dual_scenario_classification.md` and `engine/loadtest/BENCHMARKS.md`. Distinct from PL-13's restart-window scope; addresses steady-state write-queue saturation. 2026-05-28 framing: for v1.0.2 we accept the documented batching workaround (realistic-batch hits 1,483 docs/sec, well within typical Algolia-migrator patterns) and defer multi-consumer fan-out to v1.1.
## Post-Launch

HA convergence posture is framed as **bounded convergence** — a refinement after the L1 anti-entropy fix (`066549d5`), not an unresolved gap; the residual nginx restart-window write loss is tracked as ROADMAP **PL-8**, and the canonical narrative seam is ADR [`0004`](engine/docs2/3_IMPLEMENTATION/decisions/active/0004_ha_convergence_reversal.md).

See [`ROADMAP.md#open--not-yet-implemented`](ROADMAP.md#open--not-yet-implemented) for post-launch work (search HA design, durable analytics rollups, runbooks, mobile dashboard, OWASP deep pass).
